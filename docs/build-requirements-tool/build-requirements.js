const XLSX = require('xlsx')

// ============================================================
// Date lookups -- derived from real filesystem mtimes (this repo has
// no git history to date-stamp requirements against), not guessed.
// Migration dates: `stat -c '%y' deployment/migrations/*.sql`.
// Spec dates: `find e2e/tests -iname '*.spec.ts' -printf '%TY-%Tm-%Td'`.
// ============================================================
const MIGRATION_DATE = {}
;[
  [[1, 3], '2026-08-06'],
  [[4, 10], '2026-08-07'],
  [[11, 13], '2026-08-08'],
  [[14, 27], '2026-08-09'],
  [[28, 29], '2026-08-10'],
  [[30, 30], '2026-08-14'],
  [[31, 32], '2026-08-15'],
  [[33, 35], '2026-08-16'],
  [[36, 40], '2026-08-17'],
  [[41, 44], '2026-08-18'],
  [[45, 47], '2026-08-19'],
  [[48, 48], '2026-08-20'],
  [[49, 52], '2026-08-21'],
  [[53, 56], '2026-08-22'],
  [[57, 63], '2026-08-23'],
  [[87, 88], '2026-09-01'],
  [[89, 104], '2026-09-02'],
  [[105, 106], '2026-09-03'],
].forEach(([[lo, hi], date]) => {
  for (let n = lo; n <= hi; n++) MIGRATION_DATE[n] = date
})

// Requirements built with no new migration (app-layer only / read of
// an existing view) -- dated to when they actually shipped this
// session rather than left blank.
const NO_MIGRATION_DATE = '2026-08-23'

function dateForMigrations(migStr, fallback = NO_MIGRATION_DATE) {
  const nums = String(migStr).match(/\d{3}/g)
  if (!nums || nums.length === 0) return fallback
  const latest = Math.max(...nums.map((n) => parseInt(n, 10)))
  return MIGRATION_DATE[latest] || fallback
}

const SPEC_DATE = {
  'ecosystem-search-coverage.spec.ts': '2026-08-20', 'freeagent-trust-signal.spec.ts': '2026-08-20',
  'greyin-score-in-candidate-search.spec.ts': '2026-08-20', 'greymatters-cross-app-surfacing.spec.ts': '2026-08-21',
  'header-auth-state.spec.ts': '2026-08-20', 'hub-dashboard.spec.ts': '2026-08-20', 'logout-flow.spec.ts': '2026-08-20',
  'navigation.spec.ts': '2026-08-20', 'nl-search.spec.ts': '2026-08-23', 'pivoter-mentor.spec.ts': '2026-08-20',
  'reentry-mentor.spec.ts': '2026-08-20', 'referral-cross-app.spec.ts': '2026-08-22', 'session-persistence.spec.ts': '2026-08-19',
  'sso.spec.ts': '2026-08-20', 'trust-signals.spec.ts': '2026-08-20', 'activity-feed.spec.ts': '2026-08-22',
  'admin.spec.ts': '2026-08-19', 'ai-quality-score.spec.ts': '2026-08-21', 'auth-and-gigs.spec.ts': '2026-08-16',
  'checkout-payment.spec.ts': '2026-08-12', 'earnings-and-payouts.spec.ts': '2026-08-11', 'escrow-workflow.spec.ts': '2026-08-11',
  'experience-gate.spec.ts': '2026-08-17', 'gigs-filters.spec.ts': '2026-08-21', 'image-upload.spec.ts': '2026-08-11',
  'mentor-monetization.spec.ts': '2026-08-23', 'mentor-sessions.spec.ts': '2026-08-23', 'mobile-nav.spec.ts': '2026-08-17',
  'notifications.spec.ts': '2026-08-11', 'order-history.spec.ts': '2026-08-21', 'order-lifecycle.spec.ts': '2026-08-11',
  'password-reset.spec.ts': '2026-08-12', 'skill-ratings.spec.ts': '2026-08-22', 'top-tier-badge.spec.ts': '2026-08-23',
  'webhook-payment-captured.spec.ts': '2026-08-12', 'auth.spec.ts': '2026-08-11', 'candidates.spec.ts': '2026-08-17',
  'companies.spec.ts': '2026-08-11', 'company-reviews.spec.ts': '2026-08-23', 'employer-settings.spec.ts': '2026-08-21',
  'employment-history.spec.ts': '2026-08-22', 'enterprise-solutions.spec.ts': '2026-08-18', 'gate-settings-update.spec.ts': '2026-08-21',
  'governance-voting.spec.ts': '2026-08-17', 'greymatters-cross-posting.spec.ts': '2026-08-23', 'homepage.spec.ts': '2026-08-19',
  'job-alerts.spec.ts': '2026-08-21', 'job-edit.spec.ts': '2026-08-21', 'jobs-and-applications.spec.ts': '2026-08-23',
  'jobs-filters.spec.ts': '2026-08-21', 'messaging.spec.ts': '2026-08-11', 'pivoter.spec.ts': '2026-08-18',
  'reentry.spec.ts': '2026-08-19', 'referral.spec.ts': '2026-08-17', 'resume-parsing.spec.ts': '2026-08-11',
  'salary-trends.spec.ts': '2026-08-22', 'salary-trend-watch.spec.ts': '2026-08-23', 'seo.spec.ts': '2026-08-23',
  'skill-endorsements.spec.ts': '2026-08-22', 'subscription-gate.spec.ts': '2026-08-17', 'verified-expert-gate.spec.ts': '2026-08-17',
  'weekly-digest.spec.ts': '2026-08-23', 'written-recommendations.spec.ts': '2026-08-23', 'admin-digest-send.spec.ts': '2026-08-21',
  'admin-sweep.spec.ts': '2026-08-21', 'comments-and-categories.spec.ts': '2026-08-11', 'newsletter-and-search.spec.ts': '2026-08-15',
  'post-cms.spec.ts': '2026-08-11', 'tags.spec.ts': '2026-08-22', 'tips.spec.ts': '2026-08-12', 'admin-project-delete.spec.ts': '2026-08-21',
  'discussions.spec.ts': '2026-08-11', 'karma-and-anonymous.spec.ts': '2026-08-12', 'members.spec.ts': '2026-08-11',
  'mentor-session-link.spec.ts': '2026-08-23', 'admin-llm.spec.ts': '2026-08-21', 'applications.spec.ts': '2026-08-19',
  'ask-close.spec.ts': '2026-08-21', 'greyin-score.spec.ts': '2026-08-19', 'people-directory.spec.ts': '2026-08-19',
  'projects-and-asks.spec.ts': '2026-08-19', 'project-shipped.spec.ts': '2026-08-20', 'track-record-badge.spec.ts': '2026-08-23',
  'track-record.spec.ts': '2026-08-19', 'verification.spec.ts': '2026-08-20', 'worked-together.spec.ts': '2026-08-23',
  'notification-bell.spec.ts': '2026-09-02',
}

function datesForSpecs(specStr) {
  const names = String(specStr).match(/[\w-]+\.spec\.ts/g)
  if (!names || names.length === 0) return ''
  const dates = [...new Set(names.map((n) => SPEC_DATE[n]).filter(Boolean))].sort()
  return dates.join(' / ')
}

// ============================================================
// Functional Requirements — hierarchical: Section > Sub-area > Requirement
// ============================================================
const functionalRaw = [
  ['Section', 'Sub-area', 'Req ID', 'Requirement', 'Migration(s)'],

  // Platform-wide / cross-pillar
  ['Platform-Wide', 'Auth & Profiles', 'FR-PW-01', 'Users can sign up, verify email, log in/out across all 6 apps sharing one Supabase auth instance', '001'],
  ['Platform-Wide', 'Auth & Profiles', 'FR-PW-02', 'A single profiles row per user carries role, years_experience, and per-pillar fields shared across apps', '001, 007'],
  ['Platform-Wide', 'Notifications', 'FR-PW-03', 'A shared notifications table + SECURITY DEFINER triggers deliver in-app notifications for cross-cutting events', '017, 051'],
  ['Platform-Wide', 'Messaging', 'FR-PW-04', 'Users can send/receive direct messages within a pillar', '021'],
  ['Platform-Wide', 'Ecosystem Search', 'FR-PW-05', 'A unified platform_search_index aggregates jobs/gigs/posts/discussions/projects for cross-pillar keyword search', '024, 037'],
  ['Platform-Wide', 'Ecosystem Search', 'FR-PW-06', 'Natural-language search extends the index with a person-level platform_people_index and an LLM query-to-filter step (intent: people vs content)', '060, 061'],
  ['Platform-Wide', 'Trust & Reputation', 'FR-PW-07', 'A unified Greyin Score blends StackWorks/FlexPro/Salt & Pepper evidence with Bayesian shrinkage and headcount weighting', '036'],
  ['Platform-Wide', 'Trust & Reputation', 'FR-PW-08', '"Verified Expert" gate combines years_experience and Greyin Score threshold, governed by a votable governance threshold', '038, 041'],
  ['Platform-Wide', 'Activity & Follow Graph', 'FR-PW-09', 'Users can follow other users platform-wide (user_follows) and see an aggregated activity feed of followed people’s actions', '053'],
  ['Platform-Wide', 'Activity & Follow Graph', 'FR-PW-10', '"People you’ve worked with" surfaces real collaboration history (accepted StackWorks applications, completed FlexPro orders): as clickable, pillar-labeled follow suggestions on StackWorks /people and Salt & Pepper /members; as an inline "worked together" note on the specific person\'s own profile (greyin-b2b/candidates, freeagent/sellers); and as one canonical cross-pillar list on the Greyin Hub dashboard, rather than duplicated per app', '059'],
  ['Platform-Wide', 'Skill Signals', 'FR-PW-11', 'A user can endorse a skill listed on another member’s profile, gated to a real verified interaction (a real accepted StackWorks collaboration or completed FlexPro order, via the collaborators view)', '054, 064'],
  ['Platform-Wide', 'Skill Signals', 'FR-PW-12', 'A 1-5 star skill rating can be left by a real FlexPro gig counterpart or StackWorks collaborator after an interaction gate (chat/application-accepted, revisable after a verified outcome)', '054'],
  ['Platform-Wide', 'Recommendations', 'FR-PW-13', 'A member can write a free-text recommendation for another member, gated to a real verified interaction (the same collaborators-view check as skill endorsements); it stays pending until the recipient approves it, then shows publicly on the recipient’s candidate profile', '057, 065'],
  ['Platform-Wide', 'Recommendations', 'FR-PW-14', 'A recommendation’s body/authorship cannot be altered by the recipient during approval — only its status may change', '057'],
  ['Platform-Wide', 'AI / LLM Infrastructure', 'FR-PW-15', 'A pluggable LLM provider registry (Anthropic/OpenAI/Ollama) with per-feature flags drives AI-assisted quality scoring and search parsing, degrading gracefully with no provider configured. Admin-configured platform-wide at Greyin Hub\'s /admin/llm (moved from StackWorks 2026-08-24 — the registry feeds scoring across GreyMatters/Salt & Pepper/FlexPro, not a StackWorks-specific concern)', '048, 050, 061'],
  ['Platform-Wide', 'Referrals', 'FR-PW-16', 'Members can refer candidates to jobs; the referrer is notified of application status changes', '040'],
  ['Platform-Wide', 'Career Transitions', 'FR-PW-17', 'Pivoter track: members changing domains can flag pivot intent and be matched with mentors', '042'],
  ['Platform-Wide', 'Career Transitions', 'FR-PW-18', 'Re-entry track: members returning to work after a gap can flag re-entry and be matched with mentors', '045'],
  ['Platform-Wide', 'Mentorship', 'FR-PW-19', 'Verified Experts can advertise paid/unpaid mentor sessions on a public calendar with discrete pre-set slots', '043, 056'],
  ['Platform-Wide', 'Mentorship', 'FR-PW-20', 'Followers can request/book a mentor session within an advertised from/to date window; payment is captured for paid sessions', '056'],
  ['Platform-Wide', 'Salary Intelligence', 'FR-PW-21', 'Salary trend graphs (by role/level/experience/location) are generated from k-anonymized (>=3 samples) aggregates of employment history + job salary ranges', '055'],
  ['Platform-Wide', 'References', 'FR-PW-22', 'A candidate can list a reference from any existing Greyin profile (deliberately not gated to a verified interaction — references often corroborate history outside the platform), tagged with a relationship type (in-platform task, ex-colleague, current colleague, other) plus free text. An employer with a real application from that candidate can request a private check tied to that application; the referee’s response goes straight to the employer and is never visible to the candidate. Any real in-platform tie (via collaborators) is shown as additional verified context, not a gate', '065, 066'],
  ['Platform-Wide', 'Activity & Follow Graph', 'FR-PW-23', 'Greyin Hub\'s dashboard shows a personal, day-level activity calendar (a GitHub-contribution-graph-style heatmap) aggregating the viewer\'s own cross-pillar activity, privacy-scoped to the caller via a WHERE user_id = auth.uid() view rather than app-side filtering alone. Distinct from FR-PW-09\'s activity feed (followed-users\' activity, public-shaped) -- this is the viewer\'s own history only. Found during a full code sweep -- real, tested, never given its own FR row until now', '046, 047'],
  ['Platform-Wide', 'Trust & Reputation', 'FR-PW-24', 'A member can add a self-reported "peer-confirmed project" (title/company/dates, off-platform work) and tag other existing Greyin members as teammates. Each tagged teammate must independently confirm before the tag is public -- an unconfirmed tag is visible only to the tagger and the tagged member, never a third party. Deliberately never called "verified" anywhere in the UI (reserved for transaction-backed evidence) -- the UI-facing term is "peer-confirmed" throughout', '089'],
  ['Platform-Wide', 'Trust & Reputation', 'FR-PW-25', 'Once mutually confirmed, project teammates can rate each other\'s contribution (1-5, "Did not meet expectations" through "Exceeded expectations") -- gated to both sides already being confirmed members of the same project. Feeds a separate peer_score on greyin_scores, deliberately never blended into the platform-verified greyin_score/platform_composite/is_verified_expert gate, which stays computed only from StackWorks/FlexPro/Salt & Pepper/GreyMatters evidence exactly as before. The rating control itself carries an inline honesty-notice tooltip warning that a reciprocal high rating flags the whole project and shows on both profiles as a grade-inflation pattern -- added the same day the reciprocity-flagging mechanism (FR-PW-26) shipped, so the deterrent is visible at the exact moment the behavior it discourages would happen', '089'],
  ['Platform-Wide', 'Trust & Reputation', 'FR-PW-26', 'Peer contribution ratings are anonymous at the RLS layer, not just by app convention: a regular member can see only ratings they personally gave, never who rated whom else. Employers/admins additionally get two purpose-built aggregate views: peer_rater_reliability (a given rater\'s average rating given, % top-rating, and what share of their given ratings are part of a mutual pair where both sides rated each other 4+) and peer_project_reciprocity_flags (which specific project a reciprocal high-rating pair occurred on) -- surfaced as an "Employer view: rating pattern" panel and a per-project "⚠ Possible reciprocal rating" badge on the candidate profile, to guard against quid-pro-quo grade-inflation between colluding peers', '090, 091'],
  ['Platform-Wide', 'Admin & Moderation', 'FR-PW-27', 'An admin can browse peer-confirmed projects (Greyin Hub /admin/peer-projects, flagged reciprocal-rating projects sorted first via peer_project_reciprocity_flags) and delete one, which cascades to its members and ratings -- the moderation surface flagged as missing when the peer-projects feature first shipped, closed the same day', '089'],
  ['Platform-Wide', 'Admin & Moderation', 'FR-PW-28', 'An admin can run an idempotent cleanup that removes e2e-suite-generated content (title prefix "E2E ") from posts/builder_projects/discussions/gigs/jobs in one action from Greyin Hub /admin, with a per-table result breakdown shown inline -- the cleanup route recommended by the Emergent gap audit (docs/emergent_deployment_gap.md) after live test-data pollution was found on GreyMatters\' public blog', 'none'],
  ['Platform-Wide', 'Navigation', 'FR-PW-29', 'Every pillar app (DeepEdge, FlexPro, Salt & Pepper, StackWorks, GreyMatters, Longlist) carries an explicit "Home" nav link (desktop + mobile) back to the Greyin Hub, distinct from the logo\'s own implicit home-link and from the "More Platforms" dropdown -- the Hub itself was never one of the 6 PILLARS entries a pillar app\'s own dropdown lists, so there was previously no labeled way back to it from inside a pillar app at all', 'none'],
  ['Platform-Wide', 'Dashboard', 'FR-PW-30', 'Every app\'s /dashboard shows a real, personalized "your activity" line or tile (count-only query, plain-English sentence, correct singular/plural) instead of static placeholder copy or a hardcoded 0 -- extended from Longlist\'s pre-existing pattern (the only app that already had this) to DeepEdge (real applications count), FlexPro (active gigs + pending orders), Salt & Pepper (discussions started), StackWorks (asks posted, Builder-only), and GreyMatters (published posts). Emergent gap-audit item #10', 'none'],
  ['Platform-Wide', 'Wishlist', 'FR-PW-31', 'A public feature wishlist (feature_requests + feature_request_upvotes, mirrors project_upvotes\' composite-PK/trigger-maintained-count shape) -- members submit and upvote suggestions on Hub\'s /wishlist; admin manages status (open/planned/shipped/declined) and exports CSV from /admin/wishlist. Emergent gap-audit item #5', '101'],
  ['Platform-Wide', 'Feedback', 'FR-PW-32', 'A centralized feedback submission + admin reply loop (platform_feedback) on Hub\'s /feedback, reachable from a nav link on all 6 pillar apps rather than duplicated per app (SSO-enabled) -- requires login (no anonymous submission, user decision). Admin replies from /admin/feedback; the submitter is notified via the shared notifications table and sees the reply on their own /feedback history. Emergent gap-audit item #3', '101'],
  ['Platform-Wide', 'Notifications', 'FR-PW-33', 'A live notification bell (unread-count badge + dropdown, Realtime postgres_changes subscription scoped to the viewer\'s own user_id) updates instantly with no page reload, on all 6 apps that write to the shared notifications table -- in place of the previous full-page-only /notifications pattern FR-PW-03 originally shipped as. Emergent gap-audit item #4', '102'],
  ['Platform-Wide', 'Session Management', 'FR-PW-34', 'An idle-timeout session guard on all 7 apps (the 6 pillars plus the Hub): 20 minutes of no activity (mouse/keyboard/touch/scroll) shows a "Still there?" warning with a live 2-minute countdown; letting it run out signs the user out and redirects to /login with an explanatory message. Multi-tab aware via a single shared localStorage timestamp polled every second, so activity in any tab keeps every tab\'s session alive and a timeout in one tab is reflected in the others within a second, without BroadcastChannel. Deliberately NOT dismissed by passive activity once the warning is showing -- only the explicit "Stay signed in" button (or genuine activity in a different tab) does, matching AWS console/Salesforce-style idle policies rather than a bare mousemove silently re-arming it. Client-side only, no schema change -- reuses each app\'s existing /auth/logout route. Raised by direct user question ("check if there is timer for log out after inactivity") after discovering no such mechanism existed anywhere: GOTRUE_JWT_EXP is 1hr, but every app\'s middleware.ts silently refreshes an expired access token via the ~400-day refresh-token cookie on every request, so a session previously never expired from inactivity at all', 'none'],
  ['Platform-Wide', 'Navigation', 'FR-PW-35', 'A header "Explore" dropdown (pillar-specific nav links + Feedback, consolidated out of a crowded flat nav bar) and a common footer (brand + tagline, app-specific nav links, Terms/Privacy, a cross-pillar link row) on all 6 pillar apps -- previously only DeepEdge and GreyMatters had any footer at all, and only on their own homepage; FlexPro, StackWorks, Salt & Pepper, and Longlist had none anywhere. Mounted once per app in layout.tsx so it renders on every route, not just the landing page. Extended the same pattern to the Hub separately (FR-PW-31/32\'s Wishlist/Feedback links moved out of its header into its own footer, same row as its 6-pillar link list, right-aligned) at explicit user request. Found and fixed a real, load-bearing keyboard-accessibility bug while shipping this: both dropdowns\' `invisible` (CSS visibility:hidden) reveal-on-hover styling removed their links from the accessibility tree entirely when collapsed, so a keyboard-only user could never tab into "Explore" or "More Platforms" at all, only a mouse-hover user could -- fixed by adding `group-focus-within` alongside `group-hover` on both dropdowns, all 6 apps. Caught by a genuine e2e failure (cross-platform/navigation.spec.ts\'s "links to Ecosystem Search" check, itself unscoped from a hover trigger), not by manual review.', 'none'],
  ['Greyin Hub', 'Branding', 'FR-PW-36', 'Real brand assets (public/logo.png icon, public/GreyIn.png wordmark, public/logo_bgnd.png card plate) replace the generic Building2 lucide icon + plain-text "Greyin" everywhere the Hub renders its own brand mark: header, footer, login, and the browser-tab favicon (none existed before -- metadata.icons was unset). Footer specifically needed a fix beyond a straight swap: both PNGs are a fixed dark-navy color near-identical to the footer\'s own bg-gray-900, so a plain swap would have been functionally invisible there -- fixed with a `brightness(0) invert(1)` CSS filter (the standard technique for a dark-only logo asset on a dark background, since no separate light variant exists) rather than requesting or fabricating a second asset. The card-plate background is deliberately landing-page-hero-only, per explicit user instruction, composited under the icon there; the header/footer/login marks stay the bare icon with no plate. Follow-up the same day, at explicit user direction: the plate extended to the header icon too, and the hero gained a full logo+wordmark lockup (icon+plate next to the GreyIn.png wordmark) sitting above the "Built for senior professionals" eyebrow, previously just the bare icon alone.', 'none'],
  ['Platform-Wide', 'Navigation', 'FR-PW-37', 'Pillar icons (the same Building2/BookOpen/Users/Briefcase/FlaskConical/Telescope mapping the Hub\'s own homepage already used) added to two places for faster cross-pillar recognition/jump: a new icon-only jump row in the Hub\'s own header (previously the *only* way to reach a pillar from inside the Hub was the homepage\'s own grid -- every other Hub page had no cross-pillar links at all until FR-PW-36\'s header/footer work, and even then only as text), and replacing the plain color dot in every one of the 6 pillar apps\' "More Platforms" dropdown with the actual per-pillar icon. Also replaced "Switching domains" with "Pivoting" everywhere it appeared (Hub homepage card heading, the /pivoting page\'s own H1, DeepEdge profile\'s pivoter-tag prompt) to match the nav label already used elsewhere, at explicit user request.', 'none'],

  // DeepEdge (greyin-b2b)
  ['DeepEdge', 'Jobs & Applications', 'FR-EE-01', 'Employers can post/edit jobs; candidates can browse, filter, and apply with a cover letter', '001, 003'],
  ['DeepEdge', 'Jobs & Applications', 'FR-EE-02', 'Employers can review applications and update status; candidates see status changes', '001, 017'],
  ['DeepEdge', 'Company Profiles', 'FR-EE-03', 'A public company directory page lists open positions per company', '001'],
  ['DeepEdge', 'Company Reviews', 'FR-EE-04', 'Only candidates with a real application on record at a company may leave a 1-5 star + text review of that company', '058'],
  ['DeepEdge', 'Company Reviews', 'FR-EE-05', 'Company reviews are anonymous in display — reviewer identity is never rendered, only rating/text/date', '058'],
  ['DeepEdge', 'Resume & Employment History', 'FR-EE-06', 'Candidates can upload/parse a resume and maintain a structured employment history (past roles, dates, past/expected salary)', '029, 055'],
  ['DeepEdge', 'Candidate Search', 'FR-EE-07', 'Employers with an active subscription can search the candidate database; the public candidate profile itself stays open (skills/endorsements/recommendations only)', '039'],
  ['DeepEdge', 'Subscriptions', 'FR-EE-08', 'Employers subscribe via Razorpay to unlock candidate search and enterprise features', '002, 039'],
  ['DeepEdge', 'Enterprise', 'FR-EE-09', 'Enterprise solutions intake and dedicated support workflow for large employers', '044'],
  ['DeepEdge', 'Governance', 'FR-EE-10', 'Verified-expert members can vote on the Verified Expert gate’s eligibility threshold (voting itself stays on greyin-b2b/governance/threshold; the admin-facing vote summary + live gate-settings editor moved to Greyin Hub\'s /admin/threshold-votes 2026-08-24 — see FR-PW platform admin)', '041'],
  ['DeepEdge', 'Job Alerts', 'FR-EE-11', 'Candidates can subscribe to job alerts matching saved search criteria', '023'],
  ['DeepEdge', 'Enterprise', 'FR-EE-12', 'Fractional Leadership Placement: a sales-led, invoiced (not self-serve) enterprise offering -- /solutions markets it, /enterprise-contact?service=fractional_leadership captures the lead into the shared enterprise_leads pipeline (039), admin triages without a self-serve Activate action. Found during a full code sweep for features shipped but never tracked here -- real and tested, just never given its own FR row', '039, 044'],
  ['DeepEdge', 'Enterprise', 'FR-EE-13', 'Outplacement: a sales-led, invoiced (not self-serve), per-headcount enterprise offering for companies going through a layoff or restructuring -- connects departing employees into the Verified Expert ecosystem rather than a generic resume workshop. Same enterprise_leads pipeline and admin triage as FR-EE-12. One of Greyin\'s three Primary (revenue) tiers alongside Coaching (FR-PW-19/20) and the Longlist Future Hiring Pipeline (FR-LL-01/02) -- found during the same code sweep, real and tested, never tracked here until now', '039, 044'],
  ['DeepEdge', 'Jobs & Applications', 'FR-EE-14', 'A candidate can withdraw their own application (status -> \'withdrawn\'), one-way, from any non-terminal status. Emergent gap audit item #2 -- the \'withdrawn\' enum value and its UI styling already existed but no RLS policy or route ever let a candidate set it, only the employer could update application status at all. The employer is notified when it happens', '092'],
  ['DeepEdge', 'Jobs & Applications', 'FR-EE-15', 'An employer can close and reopen their own job listing without admin involvement (previously admin-only close, no reopen route at all -- Emergent gap audit item #1, its "most-impactful known issue"). Closing removes the job from the public /jobs listing and notifies every candidate with a still-open (non-terminal) application that the role has closed', '092'],
  ['DeepEdge', 'Monetization', 'FR-EE-16', 'The single fixed Starter candidate-search plan is replaced with 3 admin-priced self-serve hiring tiers (Basic/Pro/Premium, subscription_tiers, product=deepedge_hiring), each carrying named monthly credit allowances an admin sets from /admin/subscription-tiers: job_post, profile_view, contact_view, job_invite, outplacement_post, placement_request (-1=unlimited, 0=withheld from that tier). Only profile_view is wired to a live enforcement point this pass -- consume_credit() gates the new /candidates/[id] full-profile view, a second, deeper gate layered on top of the pre-existing binary "any active subscription" gate on the /candidates search list itself, which is unchanged. job_post is deliberately left unenforced: job posting is free by a prior, deliberate hybrid-gate decision (038/039, to preserve open-board liquidity) and reversing that was not confirmed with the user. contact_view/job_invite/outplacement_post/placement_request are priced and admin-configurable but have no corresponding feature built yet to consume them (outplacement today is FR-EE-13\'s sales-led lead-capture form, not a self-serve post) -- flagged inline in the admin UI itself ("not yet enforced"), not silently implied as live. Sales-led Enterprise activation (FR-EE-12/13\'s admin Activate action) grants the unlimited Premium hiring tier automatically', '096'],

  // GreyMatters
  ['GreyMatters', 'Blogging / CMS', 'FR-GM-01', 'Verified authors (12+ years experience) can write, edit, and publish long-form posts with categories/tags', '030'],
  ['GreyMatters', 'Blogging / CMS', 'FR-GM-02', 'Readers can comment on posts; anonymous posting is supported for sensitive topics', '027'],
  ['GreyMatters', 'Monetization', 'FR-GM-03', 'Readers can tip authors for valued posts', '026'],
  ['GreyMatters', 'AI Quality', 'FR-GM-04', 'An LLM-based post-quality score sweeps published posts on a configurable interval', '048, 050'],
  ['GreyMatters', 'Newsletter', 'FR-GM-05', 'Visitors can subscribe to a newsletter digest of new posts', '008'],
  ['GreyMatters', 'Newsletter', 'FR-GM-06', 'An admin can trigger a real email send (not just the in-app digest) to every newsletter subscriber, covering posts published in the last 7 days -- distinct from FR-RM-06\'s cross-pillar in-app digest, which deliberately skipped real email given the known SMTP rate-limit constraint; this one already sends real email at GreyMatters\' own subscriber volume. Found during a full code sweep -- real, tested, never given its own FR row until now', '052'],

  // Salt & Pepper
  ['Salt & Pepper', 'Community Discussions', 'FR-SP-01', 'Members (12+ years experience gate) can start and reply to discussion threads, with anonymous posting and karma scoring', '009, 025, 027'],
  ['Salt & Pepper', 'Members Directory', 'FR-SP-02', 'A public members directory lists community members with reputation score and years of experience', '035, 047'],
  ['Salt & Pepper', 'Mentorship', 'FR-SP-03', 'Mentor session links/availability are surfaced in discussions and member profiles', '043, 056'],
  ['Salt & Pepper', 'AI Quality', 'FR-SP-04', 'An LLM-based reply-quality score sweeps discussion replies on a configurable interval', '048, 050'],
  ['Salt & Pepper', 'Trust & Safety', 'FR-SP-05', 'A pre-publish moderation gate blocks a genuinely-violating discussion post or reply before it is ever inserted (harassment, hate speech, spam/scams, doxxing, explicit content) -- distinct from FR-SP-04\'s after-the-fact quality sweep. Fails open if the LLM is unreachable/unconfigured (the post goes live rather than blocking a real user) but queues the row (moderation_status=pending_check) for automatic retry every 15 minutes; a retry that comes back BLOCK is surfaced in a new admin review queue for a human to clear or delete, never auto-removed. Emergent gap-audit item #1', '098, 099'],
  ['Salt & Pepper', 'Events', 'FR-SP-06', 'Members can host and browse community events (title/description/time/location or virtual link/capacity), and RSVP or cancel -- a net-new feature Greyin had zero equivalent of (Emergent gap-audit item #7, code-level pass). The host is notified when someone RSVPs. Capacity is a UI-layer nicety (disables RSVP once full), not a DB-enforced ceiling', '100'],

  // FlexPro
  ['FlexPro', 'Gig Marketplace', 'FR-FA-01', 'Freelancers list gigs; clients browse/filter and place orders with Razorpay checkout', '002, 029'],
  ['FlexPro', 'Order Lifecycle', 'FR-FA-02', 'Orders move through a lifecycle (pending → in-progress → delivered → completed) with escrow-style payment holding and webhook-driven status updates', '002, 004, 029'],
  ['FlexPro', 'Order Lifecycle', 'FR-FA-03', 'Buyer/seller can message within an order and leave a review on completion', '005, 006'],
  ['FlexPro', 'Earnings & Payouts', 'FR-FA-04', 'Freelancers can request payouts of completed-order earnings via a configurable payout rail', '014, 020'],
  ['FlexPro', 'Skill Ratings', 'FR-FA-05', 'A client can rate the freelancer’s primary gig skills bidirectionally after a completed order', '054'],
  ['FlexPro', 'AI Quality', 'FR-FA-06', 'An LLM-based delivery-quality score reviews completed order deliverables', '048'],
  ['FlexPro', 'Experience Gate', 'FR-FA-07', 'A minimum-experience gate controls which freelancers can list gigs above a certain price tier', '039'],
  ['FlexPro', 'Monetization', 'FR-FA-08', 'A single FlexPro Pro subscription (Razorpay Subscriptions, self-serve, mirrors greyin-b2b\'s enterprise-subscription design) gates posting either a gig listing (as a freelancer) or a job listing (as a client). Browsing, applying to a job, and buying a gig all stay free -- only supply (posting) is gated, not demand. Replaces the "0%-commission" positioning entirely: that claim was never actually true (posting was free but the freelancer was already charged 2% at payout, the reverse of the claim) and is retired platform-wide (PILLARS description, signup/terms/gigs-new copy, pitch deck)', '093, 094'],
  ['FlexPro', 'Client Jobs', 'FR-FA-09', 'A client can post a job (title/description/budget); any member can apply for free with a proposed price; the client accepts one applicant (auto-rejecting the rest) or rejects individually. Accepting creates a real escrow order via the existing gig_orders machinery -- gig_id is nullable with client_job_application_id as the alternate origin, reusing the proven escrow/delivery/dispute/refund flow wholesale rather than duplicating it', '093'],
  ['FlexPro', 'Monetization', 'FR-FA-10', 'A 2% service fee applies to both sides of an accepted transaction (buyer pays amount+2%, seller nets amount-2%) -- extends the rate that was already the real gig-purchase precedent (checkout\'s buyer-side fee display + payouts\' seller-side deduction) to the new client-job-engagement path too, rather than inventing a second number for the same concept. Stored per-order (service_fee_buyer/service_fee_seller) for audit, computed at acceptance time not at posting', '093'],
  ['FlexPro', 'Trust & Safety', 'FR-FA-11', 'Either party on an order (not just the buyer, as it was) can raise a dispute for admin review -- a seller previously had no way to flag a bad-faith buyer (endless revision requests, refusal to release payment) at all. disputed_by tracks who raised it', '093'],
  ['FlexPro', 'Monetization', 'FR-FA-12', 'The single fixed posting subscription (093) is replaced with 3 admin-priced tiers (Basic/Pro/Premium, subscription_tiers, product=freeagent_posting), each carrying a monthly gig/job-posting credit allowance (subscription_tier_credits, credit_type=gig_post, -1=unlimited) enforced atomically by consume_credit() on both api/gigs/create and api/client-jobs/create -- an active subscription alone no longer guarantees unmetered posting. /subscribe lets the user pick a tier at checkout; /admin/subscription-tiers lets an admin change any tier\'s price and credit allowance live', '096'],
  ['FlexPro', 'Trust & Safety', 'FR-FA-13', 'A "Get AI suggestions" button on /gigs/new gives the seller 2-4 sentences of AI feedback on their draft listing (title/description clarity, missing scope, unclear pricing signals) before they publish -- stateless and on-demand (api/gigs/quality-check), informational only, never gates Publish and deliberately never calls consume_credit() so a suggestions click cannot burn a paid posting credit the way the real Publish action does (096). Emergent gap-audit item #4', '097'],

  // StackWorks
  ['StackWorks', 'Projects & Asks', 'FR-SE-01', 'Builders post projects and open "asks" (roles needed); Supporters browse and apply with a pitch', '030, 047'],
  ['StackWorks', 'Application Lifecycle', 'FR-SE-02', 'Builders accept/decline applications; still-pending applicants are notified if the Builder closes the ask instead', '030, 051'],
  ['StackWorks', 'Verification', 'FR-SE-03', 'Supporters submit work against verification criteria; an LLM reviews it for an AI score, and the Builder finalizes with a human score. A Builder can also self-submit a project_shipped outcome for their own project (no application_id) -- gated so the subject can never be their own reviewer (033), only an admin can', '031, 032, 033, 034, 048'],
  ['StackWorks', 'Track Record', 'FR-SE-04', 'Verified outcomes accumulate into a public, per-user track record (count + average score)', '031'],
  ['StackWorks', 'People Directory', 'FR-SE-05', 'A public People directory lists Builders/Supporters with role pill, Greyin Score, and track record', '035, 036, 047'],
  ['StackWorks', 'Skill Ratings', 'FR-SE-06', 'Builder/Supporter can bidirectionally rate each other’s skills after a gated interaction (chat/application-accepted, revisable after verified outcome)', '054'],
  ['Platform-Wide', 'Admin: LLM', 'FR-SE-07', 'Admins can add/disable/delete LLM provider rows and toggle feature flags, including per-flag sweep intervals — moved from StackWorks to Greyin Hub\'s /admin/llm 2026-08-24 (see FR-PW-15)', '048, 050'],

  // Roadmap items delivered from the competitive audit (Aug 2026) --
  // https://claude.ai/code/artifact/bcc61c58-09c9-4f5d-8789-87db703e3274
  ['Roadmap (Audit)', 'Mentorship', 'FR-RM-01', 'Mentors can run cohort/group sessions (capacity > 1), sell bulk session packages redeemable as credits, and sell paid access to a past session\'s recording', '062'],
  ['Roadmap (Audit)', 'StackWorks Portfolio', 'FR-RM-02', 'A public, unauthenticated SVG badge shows a user\'s verified-outcome count and Greyin Score, embeddable outside the app (resume, LinkedIn)', '060 (no new schema)'],
  ['Roadmap (Audit)', 'FlexPro Trust', 'FR-RM-03', 'A "Top Tier" badge auto-computed from an existing Verified Expert + high FlexPro-specific Greyin Score threshold surfaces on gig and seller pages', '036 (no new schema, auto-threshold read)'],
  ['Roadmap (Audit)', 'GreyMatters Reach', 'FR-RM-04', 'A candidate\'s published GreyMatters articles surface directly on their open DeepEdge profile', 'none (shared-schema cross-app read)'],
  ['Roadmap (Audit)', 'DeepEdge SEO', 'FR-RM-05', 'Open jobs and companies are listed in a sitemap.xml, and job detail pages carry schema.org JobPosting JSON-LD structured data', 'none (app-layer only)'],
  ['Roadmap (Audit)', 'Retention', 'FR-RM-06', 'An in-app "this week across Greyin" digest summarizes the last 7 days of cross-pillar notifications on the dashboard, in place of a real email send', 'none (reads existing notifications table)'],
  ['Roadmap (Audit)', 'Retention', 'FR-RM-07', 'Users can watch a (role, level, location) salary trend and get notified when it moves >=15% since they last checked', '063'],
  ['Roadmap (Audit)', 'DeepEdge Salary Data', 'FR-RM-08', 'Employers can mark a job\'s salary range as publicly disclosed when posting or editing it, feeding the k-anonymized salary_trends view\'s market source (previously never wired up despite the UI fields existing)', 'none (app-layer fix)'],

  // Longlist (new 7th pillar, apps/longlist) -- future roles posted
  // anonymously by companies, subscribed to by members, with a second
  // AI-surfaced candidate set. User-directed feature, 2026-08-31.
  ['Longlist', 'Future Roles', 'FR-LL-01', 'Companies post roles they expect to open 3/6/9/12 months out; the company\'s identity is withheld from every browsing member via a hand-curated, column-filtered view (future_roles_public), not app-layer filtering of the base table', '087'],
  ['Longlist', 'Future Roles', 'FR-LL-02', 'A member can subscribe to a future role as "future-interested" with one click (not an application); doing so makes their profile visible to that role\'s poster, and nothing else changes', '087, 088'],
  ['Longlist', 'Future Interests', 'FR-LL-03', 'A member can optionally state future career interests (skills + free text) on their profile, independent of any specific role, as a standing signal for AI matching', '087'],
  ['Longlist', 'AI Matching', 'FR-LL-04', 'A second, AI-surfaced candidate list (distinct from explicit subscribers) is shown to a role\'s poster, ranking platform members against the role by stated future interests -- gated by the shared llm_feature_flags registry, off by default given it surfaces profiles to an employer with no explicit opt-in for that specific role', '087'],
  ['Longlist', 'Role Lifecycle', 'FR-LL-05', 'A role\'s poster can mark it filled or expired; every subscriber is notified when a role they were interested in closes', '087'],
]
const functionalHeader = [...functionalRaw[0], 'Date Added']
const functional = [functionalHeader, ...functionalRaw.slice(1).map((row) => [...row, dateForMigrations(row[4])])]

// ============================================================
// Non-Functional Requirements
// ============================================================
const nonFunctionalRaw = [
  ['Category', 'Req ID', 'Requirement', 'How enforced', 'Migration(s)'],
  ['Security — RLS', 'NFR-SEC-01', 'Every user-writable table enforces row-level security scoped to the acting user’s auth.uid()', 'Postgres RLS policies on every migration from 003 onward', '003'],
  ['Security — RLS', 'NFR-SEC-02', 'Status-only UPDATE policies (e.g. recommendation approval) cannot be used to smuggle changes to other columns in the same statement', 'BEFORE UPDATE trigger comparing OLD/NEW on immutable fields (057) — RLS WITH CHECK alone only constrains which rows qualify, not which columns are touched', '057'],
  ['Security — Privilege', 'NFR-SEC-03', 'Privileged server-side writes that no user session should perform directly (AI scores, cross-pillar reads) use a service-role client, gated by the calling route’s own auth/ownership check, never by RLS alone', 'createServiceClient() pattern, all 6 apps’ lib/llm/client.ts', '048'],
  ['Privacy — Anonymity', 'NFR-PRIV-01', 'Company reviews never expose reviewer identity through any code path, even though reviewer_id is a real, RLS-visible column used for eligibility/uniqueness', 'App-query layer never SELECTs/joins reviewer_id for display (058)', '058'],
  ['Privacy — Anonymity', 'NFR-PRIV-02', 'Salary trend aggregates never expose an individual’s raw salary figure', 'k-anonymization: salary_trends view requires HAVING COUNT(*) >= 3 (055)', '055'],
  ['Privacy — Anonymity', 'NFR-PRIV-03', 'Anonymous discussion/post authorship is supported where the pillar allows it', 'is_anonymous flag pattern, Salt & Pepper (009, 027)', '009, 027'],
  ['Privacy — Anonymity', 'NFR-PRIV-04', 'A future role\'s posting company is never exposed to a browsing member through any code path, even though company_id/posted_by are real columns on the base table', 'future_roles_public is a hand-curated, column-filtered view (no company_id/posted_by/company join at all); the base future_roles table carries no member-facing SELECT grant, only the posting employer\'s own (087)', '087'],
  ['Privacy — Anonymity', 'NFR-PRIV-05', 'Peer contribution ratings never expose "who rated whom what" to a regular member through any code path -- real RLS enforcement, not just an app-query convention. A rater can see their own given ratings; nobody else\'s. Employers/admins alone get a purpose-built aggregate lens (rating-giving pattern + per-project reciprocity flag), never raw rater-ratee rows', 'peer_project_ratings\' SELECT policy is USING(auth.uid()=rater_id OR employer/admin) -- not USING(true) -- and the two employer-facing aggregate views (peer_rater_reliability, peer_project_reciprocity_flags) each bake their own EXISTS(...role IN (employer,admin)) gate directly into the view\'s own WHERE clause, independent of the base table\'s tightened RLS (090, 091)', '090, 091'],
  ['Reliability', 'NFR-REL-01', 'AI-assisted features (quality scores, NL search parsing) never block the primary user flow — an unconfigured or failing LLM provider degrades to a typed {ok:false} result, not an error', 'complete() never throws (048); parseSearchQuery() falls back to plain keyword search (061)', '048, 061'],
  ['Reliability', 'NFR-REL-02', 'Background/periodic work (score sweeps, expired-session finalization) runs lazily on page load rather than requiring a cron daemon', 'finalize_expired_verified_outcomes, finalize_completed_mentor_sessions, sweepUnscoredReplies (050, 056)', '050, 056'],
  ['Auditability', 'NFR-AUD-01', 'Every schema change is a numbered, forward-only SQL migration applied directly to the shared Supabase instance and verified with direct SQL before app code ships against it', 'deployment/migrations/001–063', '001, 063'],
  ['Auditability', 'NFR-AUD-02', 'Every notification-worthy state change (new application, ask closed, recommendation pending, etc.) fires a SECURITY DEFINER trigger writing to the shared notifications table', '017, 040, 051, 057', '017, 040, 051, 057'],
  ['Performance', 'NFR-PERF-01', 'Cross-pillar search and people-lookup run through a single indexed view per concern (platform_search_index, platform_people_index) rather than fan-out queries across 5 apps’ schemas', '024, 037, 060', '024, 037, 060'],
  ['Test Coverage', 'NFR-TEST-01', 'Every user-facing feature has at least one Playwright e2e spec exercising it against the real production stack, not a mocked backend', 'e2e/tests/**/*.spec.ts (125 spec files across 7 projects)', '001'],
  ['Test Coverage', 'NFR-TEST-02', 'e2e runs against prod are rate-limited (workers=4, retries=1) and a full-suite run happens at most once/day to avoid tripping the shared SMTP provider’s burst throttle', 'e2e/playwright.config.ts', '001'],
  ['Data Integrity', 'NFR-DATA-01', 'Self-referential actions are blocked at the schema level, not just in the UI (e.g. a user cannot endorse/recommend/review themselves where inapplicable)', 'CHECK constraints, e.g. written_recommendations_no_self_recommend (057)', '057'],
  ['Multi-tenancy', 'NFR-ARCH-01', 'Each of the 7 apps is independently deployable (own Docker image, own Next.js build) against one shared Supabase instance, with no server-to-server calls between apps — only hardcoded absolute cross-app links', 'deployment/frontend-stack.yml, deployment/deploy-frontend.sh', '001'],
  ['Operability', 'NFR-OPS-01', 'After applying any migration that adds/changes columns or tables, PostgREST\'s schema cache must be explicitly reloaded — NOTIFY pgrst alone is not reliably propagated through this deployment\'s connection pooling', 'docker service update --force supabase_rest, run explicitly after 062/063 once the gap was found', '062, 063'],
  ['Operability', 'NFR-OPS-02', 'Next.js metadata routes (sitemap.ts, robots.ts) that call a third-party client (Supabase) must declare fetchCache=\'force-no-store\' and revalidate=0, not just dynamic=\'force-dynamic\', to stay fresh per request', 'apps/greyin-b2b/src/app/sitemap.ts', '063'],
  ['Data Integrity', 'NFR-DATA-02', 'The Greyin Score composite only counts other-person-verified interaction as evidence for each pillar (verified StackWorks outcomes, real FlexPro order reviews, real project upvotes) — never self-generated activity (posting, replying) that requires no one else\'s participation', 'greyin_scores view saltnpepper_raw CTE filters to event_type=\'project_upvoted\' (064)', '064'],
  ['Operability', 'NFR-OPS-03', 'Every app sharing the .greyin.net SSO cookie must run a middleware.ts calling supabase.auth.getUser() on every request, so a refreshed access token\'s Set-Cookie actually persists back to the browser — without it, sessions silently go stale over time or across cross-app navigation (indistinguishable from "login/logout not working" to a real user)', 'src/middleware.ts, all 6 apps (previously only greyin-b2b had it) — found from a live user bug report and fixed 2026-08-24', 'none (app-layer only, all 6 apps)'],
  ['Data Integrity', 'NFR-DATA-03', 'Written recommendations require the same real-collaboration evidence as skill endorsements (a real accepted StackWorks collaboration or completed FlexPro order, via collaborators) — closing the same self-report gap the ethos audit found and fixed in skill_endorsements (064), missed for written_recommendations in that first pass', 'written_recommendations INSERT policy rewritten against collaborators (065)', '065'],
]
// NFR-OPS-03 has no SQL migration -- override the fallback date so it
// doesn't get mis-dated to NO_MIGRATION_DATE (2026-08-23, yesterday
// relative to when this fix actually shipped).
const nfHeader = [nonFunctionalRaw[0][0], nonFunctionalRaw[0][1], nonFunctionalRaw[0][2], nonFunctionalRaw[0][3], 'Date Added']
const nonFunctional = [nfHeader, ...nonFunctionalRaw.slice(1).map((row) => [row[0], row[1], row[2], row[3], dateForMigrations(row[4], row[1] === 'NFR-OPS-03' ? '2026-08-24' : NO_MIGRATION_DATE)])]

// ============================================================
// Traceability Matrix — requirement -> implemented -> tested -> spec file
// ============================================================
const traceabilityRaw = [
  ['Req ID', 'Requirement (short)', 'Implemented', 'Migration(s)', 'Tested', 'E2E Spec File(s)'],

  ['FR-PW-01', 'Cross-app signup/login/verify', 'Y', '001', 'Y', 'auth.spec.ts (every app)'],
  ['FR-PW-02', 'Shared profile fields', 'Y', '001, 007', 'Y', 'auth.spec.ts, homepage.spec.ts'],
  ['FR-PW-03', 'Shared notifications + triggers', 'Y', '017, 051', 'Y', 'notifications.spec.ts (greyin-b2b, greymatters, saltnpepper, freeagent)'],
  ['FR-PW-04', 'Direct messaging', 'Y', '021', 'Y', 'messaging.spec.ts (greyin-b2b, saltnpepper)'],
  ['FR-PW-05', 'Ecosystem keyword search', 'Y', '024, 037', 'Y', 'ecosystem-search-coverage.spec.ts, newsletter-and-search.spec.ts'],
  ['FR-PW-06', 'NL search (people vs content intent)', 'Y', '060, 061', 'Y', 'nl-search.spec.ts'],
  ['FR-PW-07', 'Unified Greyin Score', 'Y', '036', 'Y', 'greyin-score.spec.ts, greyin-score-in-candidate-search.spec.ts'],
  ['FR-PW-08', 'Verified Expert gate + governance', 'Y', '038, 041', 'Y', 'verified-expert-gate.spec.ts, governance-voting.spec.ts, gate-settings-update.spec.ts'],
  ['FR-PW-09', 'Follow graph + activity feed', 'Y', '053', 'Y', 'activity-feed.spec.ts (5 of 6 apps)'],
  ['FR-PW-10', 'Worked-together discovery', 'Y', '059', 'Y', 'worked-together.spec.ts, worked-together-surfacing.spec.ts'],
  ['FR-PW-11', 'Skill endorsements (verified-interaction gated)', 'Y', '054, 064', 'Y', 'skill-endorsements.spec.ts'],
  ['FR-PW-12', 'Skill ratings (gig/build interaction gated)', 'Y', '054', 'Y', 'skill-ratings.spec.ts (freeagent, stackedge)'],
  ['FR-PW-13', 'Written recommendations (verified-interaction gated, pending→approved)', 'Y', '057, 065', 'Y', 'written-recommendations.spec.ts'],
  ['FR-PW-14', 'Recommendation field immutability on approve', 'Y', '057', 'Y (direct SQL + trigger verified)', 'written-recommendations.spec.ts'],
  ['FR-PW-15', 'LLM provider registry + feature flags', 'Y', '048, 050, 061', 'Y', 'admin-llm.spec.ts, ai-quality-score.spec.ts (3 apps), admin-sweep.spec.ts, verification.spec.ts, nl-search.spec.ts'],
  ['FR-PW-16', 'Job referrals', 'Y', '040', 'Y', 'referral.spec.ts, referral-cross-app.spec.ts'],
  ['FR-PW-17', 'Pivoter track', 'Y', '042', 'Y', 'pivoter.spec.ts, pivoter-mentor.spec.ts'],
  ['FR-PW-18', 'Re-entry track', 'Y', '045', 'Y', 'reentry.spec.ts, reentry-mentor.spec.ts'],
  ['FR-PW-19', 'Mentor session calendar (advertise)', 'Y', '043, 056', 'Y', 'mentor-sessions.spec.ts, mentor-session-link.spec.ts'],
  ['FR-PW-20', 'Mentor session booking + payment', 'Y', '056', 'Y', 'mentor-sessions.spec.ts'],
  ['FR-PW-21', 'Salary trend graphs (k-anonymized)', 'Y', '055', 'Y', 'salary-trends.spec.ts, employment-history.spec.ts'],
  ['FR-PW-22', 'Employer-initiated private reference checks', 'Y', '065, 066', 'Y', 'reference-checks.spec.ts'],
  ['FR-PW-23', 'Personal cross-pillar activity calendar (Hub dashboard)', 'Y', '046, 047', 'Y', 'hub-dashboard.spec.ts'],
  ['FR-PW-24', 'Peer-confirmed projects (mutual confirmation required)', 'Y', '089', 'Y', 'peer-projects.spec.ts'],
  ['FR-PW-25', 'Peer contribution ratings -> separate peer_score', 'Y', '089', 'Y', 'peer-projects.spec.ts'],
  ['FR-PW-26', 'Peer rating anonymity + employer-only reliability/reciprocity views', 'Y', '090, 091', 'Y', 'peer-projects.spec.ts'],
  ['FR-PW-27', 'Peer projects admin moderation (browse, flagged-first, delete)', 'Y', '089', 'Y', 'peer-projects-admin.spec.ts'],
  ['FR-PW-28', 'Idempotent e2e test-data cleanup route (Emergent audit rec.)', 'Y', 'none', 'Y', 'admin-cleanup-test-data.spec.ts'],
  ['FR-PW-29', 'Explicit "Home" nav link, every pillar back to Hub', 'Y', 'none', 'Y', 'manual + live verification (see pending sheet)'],
  ['FR-PW-30', 'Real personalized "your activity" dashboard line, all 6 apps', 'Y', 'none', 'Y', 'dashboard-activity-and-gig-quality.spec.ts'],
  ['FR-PW-31', 'Public feature wishlist + upvoting + admin status/CSV export', 'Y', '101', 'Y', 'wishlist-and-feedback.spec.ts'],
  ['FR-PW-32', 'Centralized feedback submission + admin reply loop, cross-pillar notification', 'Y', '101', 'Y', 'wishlist-and-feedback.spec.ts'],
  ['FR-PW-33', 'Live notification bell (Realtime, no-reload badge + dropdown)', 'Y', '102', 'Y', 'notification-bell.spec.ts (saltnpepper, deepedge)'],
  ['FR-PW-34', 'Idle-timeout session guard (20min warning, 22min auto sign-out, multi-tab)', 'Y', 'none', 'Y', 'idle-session.spec.ts (all 7 apps)'],
  ['FR-PW-35', 'Header "Explore" dropdown + common footer on all 6 pillar apps; Hub footer relocation; keyboard-focus a11y fix', 'Y', 'none', 'Y', 'navigation.spec.ts, mobile-nav.spec.ts (all 6), homepage.spec.ts (hub)'],
  ['FR-PW-36', 'Real brand assets (icon/wordmark/card-plate) replace the generic icon + plain text on Hub header/footer/login/favicon/hero', 'Y', 'none', 'Y', 'homepage.spec.ts, idle-session.spec.ts, header-auth-state.spec.ts (hub)'],
  ['FR-PW-37', 'Pillar icons in Hub header jump row + all 6 apps\' More Platforms dropdown; "Switching domains" -> "Pivoting" everywhere', 'Y', 'none', 'Y', 'homepage.spec.ts (hub), navigation.spec.ts'],

  ['FR-EE-01', 'Post/browse/apply to jobs', 'Y', '001, 003', 'Y', 'jobs-and-applications.spec.ts, jobs-filters.spec.ts, job-edit.spec.ts'],
  ['FR-EE-02', 'Application status review', 'Y', '001, 017', 'Y', 'jobs-and-applications.spec.ts'],
  ['FR-EE-03', 'Public company directory', 'Y', '001', 'Y', 'companies.spec.ts'],
  ['FR-EE-04', 'Eligibility-gated company reviews', 'Y', '058', 'Y', 'company-reviews.spec.ts'],
  ['FR-EE-05', 'Anonymous company review display', 'Y', '058', 'Y', 'company-reviews.spec.ts'],
  ['FR-EE-06', 'Resume parse + structured employment history', 'Y', '029, 055', 'Y', 'resume-parsing.spec.ts, employment-history.spec.ts'],
  ['FR-EE-07', 'Paywalled candidate search / open profile', 'Y', '039', 'Y', 'subscription-gate.spec.ts, candidates.spec.ts'],
  ['FR-EE-08', 'Razorpay employer subscriptions', 'Y', '002, 039', 'Y', 'subscription-gate.spec.ts'],
  ['FR-EE-09', 'Enterprise solutions intake', 'Y', '044', 'Y', 'enterprise-solutions.spec.ts'],
  ['FR-EE-10', 'Gate-threshold governance voting', 'Y', '041', 'Y', 'governance-voting.spec.ts'],
  ['FR-EE-11', 'Job alerts', 'Y', '023', 'Y', 'job-alerts.spec.ts'],
  ['FR-EE-12', 'Fractional leadership lead capture + admin triage', 'Y', '039, 044', 'Y', 'enterprise-solutions.spec.ts'],
  ['FR-EE-13', 'Outplacement lead capture + admin triage', 'Y', '039, 044', 'Y', 'enterprise-solutions.spec.ts'],
  ['FR-EE-14', 'Candidate application withdrawal', 'Y', '092', 'Y', 'application-withdrawal.spec.ts'],
  ['FR-EE-15', 'Employer self-service job close/reopen', 'Y', '092', 'Y', 'job-close-reopen.spec.ts'],
  ['FR-EE-16', '3-tier hiring subscription + admin-configurable credits (profile_view enforced; job_post/contact_view/job_invite/outplacement_post/placement_request schema-ready, not yet enforced)', 'Y', '096', 'Y', 'candidate-profile-credits.spec.ts'],

  ['FR-GM-01', 'Author CMS (post/edit/publish)', 'Y', '030', 'Y', 'post-cms.spec.ts'],
  ['FR-GM-02', 'Comments + anonymous posting', 'Y', '027', 'Y', 'comments-and-categories.spec.ts'],
  ['FR-GM-03', 'Author tips', 'Y', '026', 'Y', 'tips.spec.ts'],
  ['FR-GM-04', 'AI post-quality sweep', 'Y', '048, 050', 'Y', 'ai-quality-score.spec.ts, admin-sweep.spec.ts, admin-digest-send.spec.ts'],
  ['FR-GM-05', 'Newsletter subscribe', 'Y', '008', 'Y', 'newsletter-and-search.spec.ts'],
  ['FR-GM-06', 'Admin-triggered newsletter digest email send', 'Y', '052', 'Y', 'admin-digest-send.spec.ts'],

  ['FR-SP-01', 'Discussions + karma + anonymous posting', 'Y', '009, 025, 027', 'Y', 'discussions.spec.ts, karma-and-anonymous.spec.ts'],
  ['FR-SP-02', 'Public members directory', 'Y', '035, 047', 'Y', 'members.spec.ts'],
  ['FR-SP-03', 'Mentor session links in community', 'Y', '043, 056', 'Y', 'mentor-session-link.spec.ts'],
  ['FR-SP-04', 'AI reply-quality sweep', 'Y', '048, 050', 'Y', 'ai-quality-score.spec.ts, admin-sweep.spec.ts'],
  ['FR-SP-05', 'Pre-publish moderation gate, fail-open with retry queue', 'Y', '098, 099', 'Y', 'moderation.spec.ts'],
  ['FR-SP-06', 'Community events + RSVP', 'Y', '100', 'Y', 'events.spec.ts'],

  ['FR-FA-01', 'Gig listing + checkout', 'Y', '002, 029', 'Y', 'auth-and-gigs.spec.ts, checkout-payment.spec.ts, gigs-filters.spec.ts'],
  ['FR-FA-02', 'Order lifecycle + escrow + webhooks', 'Y', '002, 004, 029', 'Y', 'order-lifecycle.spec.ts, escrow-workflow.spec.ts, webhook-payment-captured.spec.ts'],
  ['FR-FA-03', 'Order messaging + reviews', 'Y', '005, 006', 'Y', 'order-history.spec.ts'],
  ['FR-FA-04', 'Payout requests', 'Y', '014, 020', 'Y', 'earnings-and-payouts.spec.ts'],
  ['FR-FA-05', 'Bidirectional gig skill ratings', 'Y', '054', 'Y', 'skill-ratings.spec.ts'],
  ['FR-FA-06', 'AI delivery-quality review', 'Y', '048', 'Y', 'ai-quality-score.spec.ts'],
  ['FR-FA-07', 'Experience-gated gig pricing', 'Y', '039', 'Y', 'experience-gate.spec.ts'],
  ['FR-FA-08', 'FlexPro Pro subscription gates posting (either side)', 'Y', '093, 094', 'Y', 'client-jobs.spec.ts'],
  ['FR-FA-09', 'Client job posting + free application + accept/reject', 'Y', '093', 'Y', 'client-jobs.spec.ts'],
  ['FR-FA-10', '2% service fee, both sides, on accepted transaction', 'Y', '093', 'Y', 'client-jobs.spec.ts'],
  ['FR-FA-11', 'Bidirectional order disputes (buyer or seller)', 'Y', '093', 'Y', 'dispute route fix, direct verification'],
  ['FR-FA-12', '3-tier posting subscription + admin-configurable price/credits, credit-metered gig/job posting', 'Y', '096', 'Y', 'subscription-tiers.spec.ts'],
  ['FR-FA-13', 'AI gig-listing quality assist at post-time (informational, uncredited)', 'Y', '097', 'Y', 'dashboard-activity-and-gig-quality.spec.ts'],

  ['FR-SE-01', 'Projects + asks + applications', 'Y', '030, 047', 'Y', 'projects-and-asks.spec.ts, applications.spec.ts'],
  ['FR-SE-02', 'Accept/decline/close ask + notify', 'Y', '030, 051', 'Y', 'ask-close.spec.ts, applications.spec.ts'],
  ['FR-SE-03', 'AI + human verification scoring + gated self-submission', 'Y', '031, 032, 033, 034, 048', 'Y', 'verification.spec.ts, project-shipped.spec.ts'],
  ['FR-SE-04', 'Public verified-outcome track record', 'Y', '031', 'Y', 'track-record.spec.ts, project-shipped.spec.ts'],
  ['FR-SE-05', 'Public People directory', 'Y', '035, 036, 047', 'Y', 'people-directory.spec.ts'],
  ['FR-SE-06', 'Bidirectional build skill ratings', 'Y', '054', 'Y', 'skill-ratings.spec.ts'],
  ['FR-SE-07', 'Admin LLM provider/flag management', 'Y', '048, 050', 'Y', 'admin-llm.spec.ts'],

  ['FR-RM-01', 'Mentor cohorts, packages, recording resale', 'Y', '062', 'Y', 'mentor-monetization.spec.ts'],
  ['FR-RM-02', 'Public StackWorks track-record badge', 'Y', '060', 'Y', 'track-record-badge.spec.ts'],
  ['FR-RM-03', 'FlexPro Top Tier badge', 'Y', '036', 'Y', 'top-tier-badge.spec.ts, top-tier-badge-positive.spec.ts (also fixed a real bug: was checking freeagent_headcount, a platform-wide constant, instead of freeagent_evidence, this seller\'s own review count)'],
  ['FR-RM-04', 'GreyMatters cross-posting to DeepEdge', 'Y', 'none', 'Y', 'greymatters-cross-posting.spec.ts'],
  ['FR-RM-05', 'DeepEdge sitemap + JobPosting JSON-LD', 'Y', 'none', 'Y', 'seo.spec.ts'],
  ['FR-RM-06', 'In-app weekly digest', 'Y', 'none', 'Y', 'weekly-digest.spec.ts'],
  ['FR-RM-07', 'Salary trend watch/alerts', 'Y', '063', 'Y', 'salary-trend-watch.spec.ts, salary-trend-alert-fires.spec.ts'],
  ['FR-RM-08', 'Job salary_disclosed checkbox (create + edit)', 'Y', 'none', 'Y', 'salary-trend-alert-fires.spec.ts'],

  ['FR-LL-01', 'Anonymized future-role posting', 'Y', '087', 'Y', 'longlist-future-roles.spec.ts (cross-platform)'],
  ['FR-LL-02', 'Subscribe as future-interested', 'Y', '087, 088', 'Y', 'longlist-future-roles.spec.ts (cross-platform)'],
  ['FR-LL-03', 'Member-stated future interests', 'Y', '087', 'Y', 'profile-future-interests.spec.ts'],
  ['FR-LL-04', 'AI-surfaced second candidate set', 'Y', '087', 'N', 'not covered -- flag defaults off; needs an admin-enabled-flag spec as a follow-up'],
  ['FR-LL-05', 'Role filled/expired + subscriber notification', 'Y', '087', 'N', 'not covered -- follow-up'],
]
const traceHeader = [...traceabilityRaw[0], 'Date Added', 'Date Tested']
const traceability = [traceHeader, ...traceabilityRaw.slice(1).map((row) => [
  ...row,
  dateForMigrations(row[3]),
  datesForSpecs(row[5]),
])]

// ============================================================
// Pending / Partial / Deferred
// ============================================================
const pendingRaw = [
  ['Item', 'Status', 'Detail', 'Source', 'Date Identified'],
  ['Written recommendations', 'Done', 'Implemented + tested (FR-PW-13/14)', 'Greyin Feature Benchmark artifact recommendation', '2026-08-23'],
  ['Verified-reviewer company reviews', 'Done', 'Implemented + tested (FR-EE-04/05)', 'Greyin Feature Benchmark artifact recommendation', '2026-08-23'],
  ['Natural-language ecosystem search', 'Done', 'Implemented + tested (FR-PW-06)', 'Greyin Feature Benchmark artifact recommendation', '2026-08-23'],
  ['"Worked together" discovery', 'Done', 'Implemented + tested (FR-PW-10)', 'Greyin Feature Benchmark artifact recommendation', '2026-08-23'],
  ['7 competitive-audit action items', 'Done — see "Audit Roadmap" sheet', 'Mentor monetization, StackWorks badge, FlexPro Top Tier badge, GreyMatters cross-posting, DeepEdge SEO, in-app digest, salary alerts', 'Competitive audit artifact, Aug 2026', '2026-08-23'],
  ['FlexPro Top Tier badge — positive-case e2e coverage', 'Done', 'top-tier-badge-positive.spec.ts now covers it, using a new setSellerRating() e2e helper to deterministically push a seller\'s Bayesian-shrunk score past the threshold. Found and fixed a real bug while building this: the badge was checking freeagent_headcount (a platform-wide constant — how many sellers *anyone* has reviews) instead of freeagent_evidence (this seller\'s own review count), which made the >=3 evidence-depth check effectively a no-op on any live platform', 'top-tier-badge-positive.spec.ts', '2026-08-23'],
  ['Salary trend alert — full fire-on-move e2e coverage', 'Done', 'salary-trend-alert-fires.spec.ts now covers the real fire-on-move path end to end: a backdated "before" period seeded via createBackdatedJob(), a real "after" period posted through the UI (exercising the salary_disclosed fix below), and the dashboard-triggered sweep detecting the move and firing a notification', 'salary-trend-alert-fires.spec.ts', '2026-08-23'],
  ['greyin-b2b FollowButton', 'Deferred', 'DeepEdge deliberately has no FollowButton component, unlike StackWorks/Salt & Pepper/FlexPro/GreyMatters — candidate profiles use recommendations/endorsements instead of a follow relationship', 'Standing prior-session decision', '2026-08-22'],
  ['Governance vote aggregate dashboard', 'Already done (verified 2026-08-23, prior audit note was incorrect)', 'apps/greyin-hub/src/app/admin/threshold-votes/page.tsx (moved from apps/greyin-b2b 2026-08-24, see the platform-admin-consolidation row below) already fully implements this — calls get_threshold_vote_summary() (041), shows bucketed vote averages/medians by Verified-Expert status, plus the live gate-settings editor. Already has e2e coverage in governance-voting.spec.ts. The original "not built" claim in the competitive-audit conversation was a research error, not a real gap', 'Corrected 2026-08-23', '2026-08-23'],
  ['Public activity feed v1 (unauthenticated)', 'Not built', 'The activity feed (053) requires login and only shows followed users’ activity — no public, logged-out-visible feed exists. Ethos audit (2026-08-23) also flagged this as tension-prone if built as a raw public feed rather than a curated highlights page', 'Excluded from original benchmark scope', '2026-08-23'],
  ['Hourly contracts (FlexPro)', 'Not built', 'FlexPro only supports fixed-price gig orders; no hourly/time-tracked contract type', 'Excluded from original benchmark scope', '2026-08-23'],
  ['Explainable AI match rationale', 'Not built -- AI moat roadmap item', 'For every AI-Verified Search result or Longlist match, generate a plain-language "why this candidate/match" explanation alongside the score. Not cosmetic: NYC Local Law 144 and the EU AI Act\'s high-risk classification for hiring AI both push toward mandatory explainability for AI hiring tools -- this is a compliance requirement an enterprise buyer\'s legal team will increasingly ask about, not just a UX nicety. Would extend llm_feature_flags/the existing complete() call sites (048) rather than a new subsystem', 'Pitch-deck AI moat review, 2026-09-01', '2026-09-01'],
  ['Bias/fairness auditing on AI matching output', 'Not built -- AI moat roadmap item', 'A stated, active commitment to monitor AI-Verified Search and Longlist AI matching for skew against protected characteristics, not just a policy statement. Consistent with the platform\'s existing ethos -- the LinkedIn-style unverified-connection model was explicitly rejected on ethical grounds this session in favor of the collaborators-gated model. Large enterprise AI procurement increasingly requires a bias audit before signing; committing to this now is a real differentiator, not aspiration for its own sake', 'Pitch-deck AI moat review, 2026-09-01', '2026-09-01'],
  ['AI-powered mentor matching (Coaching)', 'Not built -- AI moat roadmap item', 'Suggest best-fit mentors by seniority gap, stated goals (future_interests, 087), and session-rating history -- a natural extension of data already collected, directly strengthening Coaching, one of the three Primary revenue tiers, not just the platform generally. Would reuse the existing complete()/llm_feature_flags pattern (048) and the mentor_sessions schema (056, 062)', 'Pitch-deck AI moat review, 2026-09-01', '2026-09-01'],
  ['AI-synthesized reference reports', 'Not built -- AI moat roadmap item', 'An AI summarization layer over the existing structured references/reference_requests/reference_responses tables (065) -- turning multiple raw referee responses into one consistency-flagged report for the requesting employer, saving real review time. Small, credible extension of live data, not a new data model', 'Pitch-deck AI moat review, 2026-09-01', '2026-09-01'],
  ['Referral bonus tracking', 'Not built', 'Referral notifications exist (040) but no monetary bonus/payout tracking for successful referrals. Ethos audit (2026-08-23) recommends a hire-and-tenure gate before building — every other monetized signal on the platform requires a verified transaction, and a naive referral bonus would be the first exception', 'Excluded from original benchmark scope', '2026-08-23'],
  ['jobs.salary_disclosed never set by the post-job UI', 'Fixed', 'Added a "publicly disclose this range" checkbox to both the post-job and edit-job forms, wired through both API routes (create + update). Real jobs posted through the UI now feed the salary_trends view\'s market source, and salary-trend-alert-fires.spec.ts exercises the full path end to end', 'Discovered this session, fixed 2026-08-23', '2026-08-23'],
  ['Skill endorsements are the one open/ungated social-proof signal', 'Done', 'Identified 2026-08-23, fixed 2026-08-24 per explicit user direction. Gated via a new RLS policy on skill_endorsements (064) requiring the endorser+endorsee pair to appear in the collaborators view (059, real accepted StackWorks collaboration or completed FlexPro order) — the same evidence already used for "worked together" discovery. apps/greyin-b2b/src/app/candidates/[id]/page.tsx now only renders the Endorse button when eligible, with an explanatory note otherwise; the endorse API route surfaces a real error banner instead of silently swallowing RLS failures. Found and fixed a real UX bug along the way: the endorsement count was only ever shown inside the button, so it disappeared entirely for ineligible viewers once the button got hidden — now shown as an always-visible "N endorsements" line, matching how the rating count already works. Covered end to end (positive + negative case, including a direct API bypass attempt) by skill-endorsements.spec.ts', 'Ethos audit, 2026-08-23; fixed 2026-08-24', '2026-08-24'],
  ['Salt & Pepper karma feeds Greyin Score without a verification gate', 'Done', 'Identified 2026-08-23, fixed 2026-08-24 per explicit user direction. The Greyin Score view\'s saltnpepper_raw CTE (064) now filters reputation_events to event_type=\'project_upvoted\' only — the one reputation event type that requires another real person\'s action (a genuine, per-user-deduped upvote), excluding the three self-generated event types (discussion_created, reply_given, project_created) that previously counted toward a hiring-relevant score just for a user posting their own content. reputation_scores (Salt & Pepper\'s own /members display) is untouched — posting activity is still legitimate community standing there, just no longer evidence for the cross-pillar composite. Verified via direct SQL and by saltnpepper-karma-gate.spec.ts, which confirms a builder\'s own project post does not move their score but a real upvote from a different account does', 'Ethos audit, 2026-08-23; fixed 2026-08-24', '2026-08-24'],
  ['SMTP rate limiting under e2e load', 'Parked', 'Hostinger’s 500/hr SMTP cap can be hit by heavy e2e signup volume; recommended fix is swapping to a dedicated provider (Resend/SES) — not yet actioned. This is also why FR-RM-06 shipped as an in-app digest instead of a real weekly email', 'project_smtp_rate_limit_parked.md (session memory)', '2026-08-21'],
  ['Full-suite run, 2026-08-23 (post-audit-deploy)', 'Done — zero real regressions', '166 tests run against prod, workers=4/retries=1. First-pass: 157 passed, 8 failed, 1 flaky (passed on retry). All 8 failures individually re-verified after fixes: 2 were real pre-existing bugs (see next 2 rows), 5 were a GoTrue read-after-write race under full-suite concurrency (not a regression — see next row), 1 was a genuine one-off ERR_NETWORK_CHANGED network blip. Every one of the 8 now passes in isolation.', 'Full-suite run requested after competitive-audit deploy', '2026-08-23'],
  ['GoTrue read-after-write race under full-suite load', 'Hardened, not a functional bug', 'e2e/utils/admin.ts\'s confirmTestUserEmail already retried a paginated admin-API user lookup for exactly this race (documented in its own comments), but the existing budget (15 pages deep, 10 retries) was sized for single-feature verification runs, not a genuine 166-test full run\'s signup volume. Hit 5 times in the 2026-08-23 full run (pivoter-mentor, referral-cross-app, session-persistence, sso, trust-signals — all cross-platform). Hardened to 25 pages / 15 retries; all 5 confirmed passing individually afterward, proving the underlying app features were never broken', 'Found during 2026-08-23 full-suite run', '2026-08-23'],
  ['weekly-digest.spec.ts assumed the recommendation form is always visible', 'Fixed', 'Found during the pre-full-run audit: the test wrote a recommendation between two strangers with no real collaboration, which written_recommendations\' new gate (065, same day) now blocks — the form is hidden for ineligible viewers. Fixed by seeding a real completed FlexPro order first, same pattern as written-recommendations.spec.ts/skill-endorsements.spec.ts. Verified passing individually before the full run', 'Found during pre-full-run audit, 2026-08-24', '2026-08-24'],
  ['Full-suite run, 2026-08-24 (post feature-batch: middleware fix, ethos gates, worked-together surfacing, reference checks, admin consolidation)', 'Done — zero real regressions', 'First full run of the day (once/day cap). 172 tests run against prod, workers=4/retries=1. First-pass: 159 passed, 3 flaky (passed on retry), 10 failed even after retry — all 13 in the cross-platform project, and all 13 shared one identical root error ("Could not find newly-created test user... to confirm"), the same class of issue as the 2026-08-23 GoTrue race (see previous row), just recurring worse as the cross-platform project grew today (5 specs moved/added: session-refresh-middleware, worked-together-surfacing, admin-llm, governance-voting, gate-settings-update). None were regressions in today\'s actual feature work — every touched feature (middleware, skill-endorsement/recommendation/karma gates, worked-together surfacing, reference checks, admin-panel move) had already passed its own dedicated spec individually earlier in the day', 'Full-suite run requested by user, 2026-08-24', '2026-08-24'],
  ['GoTrue admin-lookup congestion, properly fixed (not just re-hardened)', 'Fixed', 'Root-caused via direct API testing against prod (curl, service-role key): findUserIdByEmail (e2e/utils/admin.ts) was paginating GoTrue\'s admin users list 20-at-a-time, up to 25 sequential requests per lookup attempt, retried up to 15 times by confirmTestUserEmail — up to 375 sequential HTTP requests to the same admin endpoint per single user confirmation. Confirmed the endpoint accepts a single per_page=2000 request instead (returned all 200 then-current users in one call, matching x-total-count exactly, no truncation) -- with workers=4 running dozens of concurrent signups each doing the old 25-request dance, this was self-inflicted congestion on the admin API, not multi-second replication lag. Rewrote findUserIdByEmail to fetch in one request (~25x fewer requests per lookup); the outer retry loop is unchanged as a safety net, now far cheaper per attempt. Validated by re-running the entire cross-platform project (42 tests, not the full suite — the once/day cap applies to the full ~172-test run, already used today) under the same workers=4 concurrency: 42/42 passed, zero retries needed', 'Fixed and validated same day, 2026-08-24', '2026-08-24'],
  ['mentor-sessions.spec.ts input[name="title"] collision', 'Fixed', 'The pre-existing spec broke once mentor-monetization.spec.ts\'s new "Add package" mini-form (062) was added to the same manage page — a bare input[name="title"] locator started matching 2 elements. Same fix pattern as mentor-monetization.spec.ts itself: scoped to form[action="/api/mentor-sessions/gigs"]', 'Found during 2026-08-23 full-suite run', '2026-08-23'],
  ['jobs-and-applications.spec.ts / notifications.spec.ts waitForURL regex', 'Fixed', 'waitForURL(/\\/applications$/) never matched the real redirect target /employer/jobs/[id]/applications?success=1 (confirmed via page snapshot that the actual status-update feature worked correctly the whole time) — a latent, pre-existing test bug unrelated to any change this session, just never previously triggered/noticed. Fixed to waitForURL(/\\/applications\\?success=1/) in both files', 'Found during 2026-08-23 full-suite run', '2026-08-23'],
  ['Login/logout not working reliably across pillars', 'Fixed', 'Live bug reported directly by the user: logging in on Greyin Hub, staying logged in, then navigating to another pillar or logging out didn\'t reliably work. Root cause: every app\'s lib/supabase/server.ts cookie code has a comment saying failures there "can be ignored if you have middleware refreshing user sessions" — but only greyin-b2b actually had that middleware.ts (src/middleware.ts calling supabase.auth.getUser() on every request so a refreshed access token\'s Set-Cookie persists back to the browser). The other 5 apps, including Greyin Hub itself, had none — so a session open a while, or navigated across the shared .greyin.net cookie domain, could silently go stale, reading exactly as "login/logout isn\'t working" to a real user. The existing sso.spec.ts/logout-flow.spec.ts both do an immediate login-then-single-action flow too short-lived to ever need a token refresh, so neither caught it. Fixed by adding the same middleware.ts to all 5 remaining apps (greyin-hub, greymatters-blog, saltnpepper-community, freeagent-marketplace, stackedge); deployed to prod and confirmed with a new spec (session-refresh-middleware.spec.ts) that chains several real cross-pillar navigations starting from Greyin Hub before checking the session still holds, then verifies logout actually clears the shared cookie (not just redirects) and that the logout propagates to a second pillar too. sso.spec.ts and logout-flow.spec.ts re-run individually against prod with no regressions', 'User bug report, 2026-08-24; fixed same day', '2026-08-24'],
  ['Written recommendations open to any authenticated user, no interaction gate', 'Fixed', 'Left open when 064 gated skill_endorsements and the Greyin Score\'s saltnpepper input — same self-report gap, just missed in that pass. Flagged by the user while discussing whether a "references" feature would add value; fixed the same day (2026-08-24) via the identical collaborators-view gate skill_endorsements already uses (065). The "Write a recommendation" form on candidates/[id] is now hidden for ineligible viewers (with an explanatory note), matching the endorsement UI pattern; the create route surfaces an honest error instead of a generic one. Verified end to end by written-recommendations.spec.ts (rewrote to seed a real collaboration first; added a negative case for a stranger)', 'User conversation, 2026-08-24', '2026-08-24'],
  ['Employer-initiated private reference checks', 'Done', 'Follow-up to the LinkedIn "connection request" discussion below: user asked whether a reference-check feature (distinct from recommendations) would add value. Assessed as a genuinely different, stronger signal — employer asks directly, candidate never sees or edits the answer — and, per explicit user direction, deliberately NOT gated to a verified interaction the way endorsements/recommendations are, since a reference is often corroborating something outside Greyin entirely (a past job). Instead: the candidate tags the relationship from a dropdown (in-platform task, ex-colleague, current colleague, other) plus free text, and any real in-platform tie is shown as additional verified context (a "Greyin-verified: worked together via X" badge, computed once at insert time via a trigger since collaborators\' own RLS correctly won\'t let a non-participant employer query it live). New tables professional_references/reference_requests/reference_responses (065); reference_requests has no INSERT policy at all — the eligibility check spans 4 tables, so create_reference_request() (SECURITY DEFINER) is the only legitimate write path, verified directly via a blocked-bypass SQL test. Employer eligibility mirrors company_reviews: a real application at that employer\'s company. New UI: "Add as reference" on candidates/[id], "Your references" management + pending-request count on /profile, and /references/respond for the referee to answer privately. Verified end to end by reference-checks.spec.ts: a candidate lists two references (one plain, one with a real FlexPro order behind it), an employer requests a check tied to a real job application, the referee answers privately, the employer sees the response, and the candidate never does, anywhere', 'User conversation, 2026-08-24', '2026-08-24'],
  ['Platform-wide admin consolidated onto Greyin Hub', 'Done', 'Two admin panels were living on pillar-specific apps despite being genuinely platform-wide: the LLM provider/feature-flag panel (apps/*/admin/llm, llm_providers/llm_feature_flags feed AI scoring across GreyMatters/Salt & Pepper/FlexPro) was on StackWorks, and the eligibility-threshold governance summary + live gate-settings editor (apps/*/admin/threshold-votes, affects Verified Expert status across all four marketplace apps) was on greyin-b2b. Per explicit user direction ("move all platform-wide admin to one location"), both moved to Greyin Hub 2026-08-24 — the one app with no pillar affiliation of its own — under a new shared /admin index. The old locations on StackWorks/greyin-b2b now point to the new ones via absolute cross-app links rather than just disappearing. Member voting on the eligibility threshold itself stays on greyin-b2b/governance/threshold — only the admin-facing summary/editor moved. All API routes carried over unchanged (self-contained via each app\'s own absoluteUrl()). Verified end to end by 3 specs moved into e2e/tests/cross-platform/ (admin-llm.spec.ts, governance-voting.spec.ts, gate-settings-update.spec.ts), all passing against prod', 'User direction, 2026-08-24', '2026-08-24'],
  ['Longlist — 7th pillar, future roles posted anonymously', 'Done, live and e2e-covered', 'User-directed: "future roles to be hired down the line (3/6/9/12 months)" as a separate pillar, not a feature bolted onto DeepEdge. Shipped as apps/longlist (own domain, own Docker service, own middleware/SSO), migrations 087 (schema + RLS) and 088 (a real bug found by direct RLS verification: the subscription INSERT check queried the base future_roles table, which members have no SELECT grant on, so even subscribing to your own future-interest was unconditionally rejected until fixed to check future_roles_public instead). Anonymity is structural (a column-filtered view, no company_id/posted_by ever exposed), not a UI convention -- verified directly via SET LOCAL role authenticated + request.jwt.claims. AI-matching second candidate set rides the existing llm_feature_flags registry, defaulted OFF. Cross-linked into all 6 existing apps\' nav + notification-link.ts, greyin-hub\'s landing page, and greyin-b2b\'s /about page. DNS (longlist.greyin.net, configured by the user on the Hostinger panel) resolved and a real Let\'s Encrypt cert issued after force-restarting the shared Traefik service to pick up the new router -- confirmed live over HTTPS. E2E: 4 specs in tests/longlist/ + 2 in tests/cross-platform/longlist-future-roles.spec.ts (the full anonymized-post -> subscribe -> employer-sees-candidate flow, and a stranger-gets-404 negative case), 6/6 passing. Two real bugs found and fixed while getting these green, neither caught by the earlier direct-SQL-only verification: (1) PostgREST\'s schema cache was stale for the new future_roles table/profiles columns (PGRST204 "column does not exist" on every write through the Supabase JS client, even though direct psql worked fine) -- another instance of the same class NFR-OPS-01 already documents (NOTIFY pgrst alone doesn\'t reliably propagate through this deployment), fixed with docker service update --force supabase_rest; (2) a test-authoring bug in profile-future-interests.spec.ts assumed login() would land on an arbitrary expectedUrl, when it only ever waits for wherever the login route actually redirects (always /dashboard for Longlist, no role branching) -- fixed by navigating separately after login instead of asserting through it. Also hit and cleared this session\'s own SEC-017 signup rate limit (10/hour/IP, shared across all 6 apps\' signup routes since they key on the same rate_limit_attempts table and this session\'s test traffic all shares one IP) -- expected behavior working as designed, not a bug, just self-inflicted by the volume of same-day testing. FR-LL-04/05 (AI-matching, role-filled notification) remain uncovered by e2e -- flagged as a follow-up, not silently skipped.', 'User request, 2026-08-31', '2026-08-31'],
  ['Three pillars rebranded: ExpertEdge -> DeepEdge, FreeAgent -> FlexPro, StackEdge -> StackWorks', 'Done -- migrated and redeployed', 'User-directed naming pass, following on from the deck/traceability naming discussion this same session. Domains cut over (Hostinger A-records for flexpro/deepedge/stackworks.greyin.net added by the user, old expertedge/freeagent/stackedge A-records already repointed to localhost -- confirmed dead before this migration touched anything). FreeAgent specifically fixes a real trademark-collision risk (an existing UK accounting SaaS company already uses that name), not just a style preference. Scope deliberately limited to the brand/domain layer, matching the precedent already set by ExpertEdge itself (which has run as apps/greyin-b2b, branded differently from its folder, since the app was built) and by StackEdge\'s own earlier rename from Prolab (047): app folder names, Docker image names, Compose service keys, Traefik router/service IDs, e2e test directory names, and e2e helper function names (signUpFreeAgent, signUpGreyinB2B, signUpStackEdgeBuilder/Supporter) all kept as internal codenames -- renaming those would touch dozens of files for zero user-facing benefit and meaningfully risk the 125-spec e2e suite. What changed: deployment/.env + .env.example (domain env vars renamed and repointed), frontend-stack.yml (Host() rules + NEXT_PUBLIC_SITE_URL, plus updated section-header comments), every app\'s displayed brand text (SiteHeader wordmarks, page titles/metadata, hero/terms/privacy copy), the shared PILLARS array in all 7 apps\' EcosystemWidget.tsx (label + url, key left unchanged), the FeedCard.tsx/EcosystemSearchResults.tsx pillar-label maps duplicated across 5 apps, notification-link.ts\'s PILLAR_DOMAINS in 6 apps, and every literal cross-app mention (greyin-hub\'s pivoting/reentry/dashboard/admin pages, longlist\'s employer-onboarding links, greyin-b2b/about, greymatters/about). Caught via a full domain-string sweep (26 files still had lowercase href="https://expertedge.greyin.net"-style literals the brand-name text replace didn\'t touch) and a separate e2e sweep that found real breaking assertions -- most importantly playwright.config.ts\'s own baseURLs for the greyin-b2b/freeagent/stackedge projects, which would have pointed the entire e2e suite at now-dead domains if missed, plus specific getByText/name: assertions in navigation.spec.ts, homepage.spec.ts, greyin-score.spec.ts, trust-signals.spec.ts, freeagent-trust-signal.spec.ts, worked-together-surfacing.spec.ts, logout-flow.spec.ts, and reference-checks.spec.ts checking literal old brand text that the app no longer renders. All 7 apps typecheck clean post-rename.', 'User request, 2026-09-01', '2026-09-01'],
  ['Full code sweep for untracked-but-shipped features', 'Done -- 6 documentation gaps found and closed, zero implementation or test gaps found', 'User-directed, prompted by discovering Outplacement had zero FR row despite being real and tested. Method: diffed every migration (001-088) against every migration number actually cited anywhere in this file, then read the ones that came back genuinely uncited (046, 049, 052) plus a few with only passing prose mentions (025, 032-034, 044) to judge whether each was a real distinct feature or a pure fix. Also ran a blunt route-path-fragment grep across all 127 API routes against every spec file to surface untested candidates -- noisy (most hits were false positives; specs test via UI flows, not literal route strings, e.g. jobs/create genuinely is tested by jobs-and-applications.spec.ts despite not string-matching) but useful for triage. Four real, shipped, already-e2e-tested features had no FR row at all: Fractional Leadership Placement and Outplacement (both sales-led enterprise offerings, migration 044, tested by enterprise-solutions.spec.ts -- now FR-EE-12/13, and Outplacement is one of Greyin\'s three Primary/revenue tiers per the pitch deck\'s tier restructuring this same request), the personal cross-pillar activity calendar on Greyin Hub\'s dashboard (migrations 046/047, tested by hub-dashboard.spec.ts -- now FR-PW-23), and GreyMatters\' admin-triggered real-email newsletter digest send (migration 052, tested by admin-digest-send.spec.ts -- now FR-GM-06, and worth noting it sends real email despite FR-RM-06\'s cross-pillar digest deliberately avoiding email over the known SMTP rate-limit constraint; different scope, different volume, both correct). Two existing FR rows had incomplete migration citations, fixed rather than left: FR-SP-01 (Salt & Pepper karma) was missing 025, the migration that actually creates reputation_events; FR-SE-03 (StackWorks verification) was missing 032-034, which close a self-review hole and enable a Builder\'s own project_shipped self-submission -- a real workflow variant beyond what the row\'s original text described. No new engineering or test-writing was needed for any of the six -- every one was already correctly implemented and already had real e2e coverage; the gap was purely in this document, not in the product.', 'User request, 2026-09-01', '2026-09-01'],
  ['LinkedIn-style "connection request" — considered, rejected in favor of surfacing real collaborators', 'Done (alternative shipped)', 'User asked whether Greyin had a LinkedIn-style mutual connect-request feature; it didn\'t. Assessed against the platform ethos and rejected: a request/accept graph is, by construction, two strangers mutually self-declaring a relationship with zero verification — the same class of gap the skill-endorsement and Salt & Pepper karma gates (064) were built to close, not reopen. DMs are already open to any authenticated user and profiles are already unpaywalled, so a connection wouldn\'t even unlock anything LinkedIn-equivalent. Shipped the on-ethos alternative instead: surfaced the collaborators view (059, real accepted StackWorks collaborations + completed FlexPro orders) properly rather than adding a new unverified signal. (A) the existing "people you\'ve worked with" pill widgets on StackWorks /people and Salt & Pepper /members now link to the person\'s profile and show which pillar via; (B) individual profile pages (greyin-b2b/candidates/[id], freeagent/sellers/[id]) now show an inline "You\'ve worked together via X" note when the viewer and the profile actually share a collaborators row — freeagent/sellers/[id] had zero collaborators awareness before; (C) Greyin Hub\'s dashboard now has one canonical cross-pillar "People you\'ve worked with" section, rather than duplicating a per-app module (the same "5 independently-drifting copies" pattern that caused the login/logout middleware bug above). Verified end to end by worked-together-surfacing.spec.ts: a real completed FlexPro order makes the collaborator show up correctly on Hub and on the seller profile, and confirms a stranger with no real interaction sees neither', 'User request, 2026-08-24', '2026-08-24'],
  ['Peer-confirmed projects (off-platform work history) + anonymous peer ratings + employer reciprocity detection + admin moderation', 'Done, live and e2e-covered -- admin moderation UI added same day, remaining gaps flagged below, not yet closed', 'User proposed the use case (add off-platform projects, tag real teammates, rate contribution) and asked for a business-value assessment before building. Assessed as solving a real cold-start gap (every existing trust signal requires an on-platform transaction; a senior member\'s entire pre-Greyin career contributes nothing) with a real viral-acquisition angle (tagging a non-member is an invite hook), conditioned on three things the initial assessment flagged as required, not optional: (1) never call it "verified" -- reserved for transaction-backed evidence elsewhere on the platform, this is "peer-confirmed" throughout; (2) mutual confirmation as a real gate, not a courtesy -- a tag stays private until the tagged member actively confirms; (3) ratings anonymous by default. User then explicitly asked for the peer_score to be a fully separate number from the platform-verified Greyin Score, never blended -- built that way (089): platform_composite/greyin_score/is_verified_expert still compute only from StackWorks/FlexPro/Salt&Pepper/GreyMatters, peer_score is a sibling column nobody\'s gate reads. User then raised the sharpest requirement: ratings must be genuinely anonymous at the RLS layer (not just an app convention) for regular members, but employers specifically need to see a given rater\'s rating-giving PATTERN, to catch reciprocal quid-pro-quo grade-inflation between colluding peers. Initially shipped following company_reviews\' (058) "anonymity is an app convention" precedent, USING(true) -- caught as insufficient against this specific requirement before rollout and tightened (090) to real row-level restriction (self-caught, logged as SEC-039). Two new employer/admin-gated aggregate views followed: peer_rater_reliability (a rater\'s overall pattern -- avg rating given, % top-rating, % of ratings that are part of a mutual pair where both sides rated each other 4+) and peer_project_reciprocity_flags (090, 091 -- the same detection at per-project granularity, surfaced as a "⚠ Possible reciprocal rating" badge on the specific flagged project, added after the user asked for project-level flagging specifically rather than only an aggregate rater-level number). Schema: peer_projects/peer_project_members/peer_project_ratings (089), collaborators view extended with a third \'peer\' source (mutually-confirmed pairs only), greyin_scores extended with peer_score/peer_evidence/peer_headcount as trailing columns (CREATE OR REPLACE VIEW\'s column-position constraint required restructuring the query into a `gated` CTE to append them after is_verified_expert rather than before years_experience -- caught by the DB itself rejecting the first attempt, not found via review). UI: greyin-hub/dashboard (add project, tag via a platform_people_index-backed live search, confirm/decline pending tags, rate confirmed teammates using performance-review-style labels -- 1="Did not meet expectations" through 5="Exceeded expectations" -- rather than a bare number) and greyin-b2b/candidates/[id] (employer-only rating-pattern panel + per-project reciprocity flag; the peer-confirmed project list itself is public to any viewer, only the pattern/flag are employer-gated). Verified via direct RLS simulation as rater/non-rater/employer/non-employer (mutual-pair reciprocity math confirmed correct: a rater with one reciprocal + one one-directional rating correctly shows 50%, a rater with only a reciprocal rating shows 100%) and end to end via peer-projects.spec.ts (real signups, real UI tag/confirm/rate flow across two browser contexts, employer sees the flag, a regular member does not). One real app-layer bug found and fixed during e2e verification that had nothing to do with the schema: a locator matching on displayed name text picked up the dashboard\'s unrelated "People you\'ve worked with" module (which independently renders the same names) instead of the intended rating form -- fixed by scoping the test locator to the rating form\'s own ratee_id hidden input instead of name text; several rebuild/redeploy cycles and live server-side debug logging were needed to isolate this from a false "stale data" hypothesis before finding the real cause. GAPS/IMPROVEMENTS RECOMMENDED BEFORE WIDER ROLLOUT, not yet built: (a) reciprocity detection is pairwise only -- a 3+-account round-robin ring (A rates B, B rates C, C rates A, never a direct mutual pair) evades both peer_rater_reliability and peer_project_reciprocity_flags entirely; (b) no rate limiting on peer_projects/peer_project_members/peer_project_ratings creation -- unlike signup (SEC-017), nothing stops spamming fake projects/tags/ratings; (c) no path for a CONFIRMED member to later withdraw their confirmation (only a still-pending tag can be removed, by the creator) -- worth a considered decision on what should happen to any ratings already given if a withdrawal were added; (d) company name on a peer project is free text with no employer attestation -- correctly framed as peer-confirmed not employer-verified, but worth stating plainly as a known limitation rather than an implicit one; (e) the 50%-mutual-high-rating-pct threshold used for the employer UI\'s visual warning is an unvalidated heuristic (chosen at build time, not calibrated against any real usage data, since the platform has none yet); (f) peer_score\'s Bayesian-shrinkage constant (m_peer=3) was set by pattern-matching StackWorks\' own m=3, not derived from real data either; (g) e2e coverage is otherwise still happy-path only -- declining a tag and the "creator can only edit before anyone confirms" lock remain untested. FOLLOW-UP THE SAME DAY -- admin moderation (FR-PW-27), CLOSED: user asked to check the admin flow specifically and add it to the test suite -- this was gap (e) in the original list ("no admin moderation UI... only raw SQL"), now removed from the open list above. Built /admin/peer-projects on Greyin Hub (new card on the existing platform-wide /admin index, same "no pillar affiliation of its own" rationale as the LLM-provider and eligibility-governance panels already there) -- lists the 50 most recent projects, flagged ones (via peer_project_reciprocity_flags) sorted first with the same "⚠ Possible reciprocal rating" badge an employer sees, delete cascades to members/ratings. RLS\'s existing admin-DELETE policy (089) is the real enforcement; the page\'s own role check is just UX (redirect before a non-admin sees the button). Verified end to end via peer-projects-admin.spec.ts: a non-admin visiting the URL directly is redirected to /dashboard, and an admin sees a real flagged project (same tag/confirm/mutual-rate flow as peer-projects.spec.ts) and successfully deletes it. This also closes the admin-delete half of the former e2e-coverage gap -- (g) above is only decline/edit-lock now, not admin-delete.', 'User request, 2026-09-02', '2026-09-02'],
  ['FlexPro revenue model rebuild: client-posted jobs, subscription-gated posting, 2% both-sides service fee, bidirectional disputes', 'Done, live and e2e-covered', 'Started from a chain of user questions rather than a single spec: (1) asked to check whether FreeAgent had any "expert posts a gig, others apply" mechanism -- confirmed it did not (only seller-lists-a-listing, buyer-purchases-directly existed); (2) user then specified the real intended model -- experts can ALSO post as a client (not just offer services as a freelancer), both sides pay a subscription to post, plus a service fee per accepted transaction, and if that means "0%-commission" is no longer accurate, retire the wording; explicitly asked for a red-flag/dispute mechanism for both sides ("to avoid pay conflicts/bad deliverables upon breach of trust"); confirmed StackWorks stays a free crowdwork model, unaffected. This surfaced a real pre-existing discrepancy before any of it was built: the "0%-commission" claim (PILLARS description, signup/terms/gigs-new copy, pitch deck) was already false in the OPPOSITE direction -- posting was free but the freelancer was already being charged a real 2% platform fee at payout. Two architectural decisions confirmed with the user before writing schema: one unified subscription (not two) covering both post-as-freelancer and post-as-client, matching the platform\'s existing "one identity, many roles" pattern; and the service fee split both sides rather than one, smaller each. The 2% rate itself was NOT invented for this feature -- it\'s the exact rate the existing gig-purchase checkout/payout flow already used on both sides (buyer-side fee shown at checkout, seller-side deduction at payout), so this pass just named and extended an already-real number rather than picking a new one. Schema (093): freeagent_subscriptions (mirrors 039\'s company_subscriptions design exactly, user-scoped since FreeAgent has no company concept), client_jobs + client_job_applications (free to apply, no subscription needed -- only posting either kind of listing is gated), and gig_orders extended with a nullable gig_id + client_job_application_id as an alternate origin so client-job engagements reuse the ENTIRE existing escrow/delivery/dispute/refund/Razorpay machinery wholesale rather than duplicating a ~35-column table -- confirmed safe before writing by reading every existing gig_orders RLS policy first: all of them key off buyer_id/seller_id directly, never gig_id, so zero RLS changes were needed there. 094 gates the pre-existing gigs INSERT policy the same way (posting a gig was never gated before, despite the old "0%" claim implying some model existed). Two real pre-existing bugs found only because this feature exercised gig_orders in a genuinely new way, both fixed in 095, and neither specific to this feature -- a pre-claim-then-attach-Razorpay-order pattern (api/orders/create\'s own "preClaimedOrderId" path, already used by mentor-session bookings) was silently broken by 068/073\'s immutable-fields trigger, which blocked razorpay_order_id\'s legitimate first-time NULL-to-value transition, not just later tampering -- caught immediately by e2e (mockRazorpayCheckout succeeding, then the DB update failing with the trigger\'s own exception), and very likely a live, if less-exercised, gap in mentor-session checkout too, not something newly introduced; separately, the DB trigger enforcing the \'disputed\' transition was ALSO still buyer-only underneath the app-layer route fix below, which alone would not have been sufficient. Full pg_get_functiondef of the original trigger was read before writing the replacement, to guarantee every existing transition rule (revision requests, admin dispute resolution, buyer cancellation, seller delivery) was preserved verbatim and not accidentally dropped. api/orders/dispute/route.ts\'s buyer-only restriction was lifted (either buyer_id or seller_id can now raise a dispute, disputed_by records who) with matching UI added to orders/[id]/page.tsx for the seller\'s own waiting/revision states, which previously showed the seller nothing at all once they\'d delivered. UI: /subscribe (mirrors greyin-b2b\'s own subscription checkout page, Razorpay Subscriptions), /client-jobs (browse/post/detail with apply form + owner accept/reject), /client-jobs/[id]/pay/[orderId] (a job-engagement-specific checkout, no package selection since the accepted application already fixed one price) reusing api/orders/create/verify with a new clientJobEngagement branch rather than a parallel payment flow. All "0%-commission" wording retired platform-wide (PILLARS.description in all 7 apps, FreeAgent\'s signup/terms/gigs-new copy, and the pitch deck\'s customer-opportunity and pillar-summary slides) in favor of accurately describing the real model. Verified end to end via client-jobs.spec.ts (real subscription grant, gate correctly blocks/allows posting, free application, accept creates a real gig_orders row via the real UI + mocked Razorpay checkout.js, stored amount/service_fee_buyer/service_fee_seller read back and confirmed exactly correct -- 9000/180/180 for a 9000 proposed price) plus re-running the pre-existing payment-critical suite after the shared-infrastructure changes: checkout-payment.spec.ts and all 3 escrow-workflow.spec.ts cases (revision/redelivery, buyer cancel+refund, admin dispute resolution) pass clean, confirming 095\'s trigger rewrite preserved every existing transition rule. The new subscription gate on gig-posting is a real, deliberate breaking change to 14 pre-existing e2e specs that post a gig as a normal part of their own setup (checkout-payment, escrow-workflow, gigs-filters, earnings-and-payouts, webhook-payment-captured, order-lifecycle, notifications, auth-and-gigs, admin, ai-quality-score, image-upload, top-tier-badge, top-tier-badge-positive, activity-feed) -- all 14 updated (grantFreeagentSubscription called before posting, same "grant directly, don\'t drive the real subscription-checkout UI in e2e" precedent already established for greyin-b2b\'s own enterprise subscriptions) and 5 of the 14 re-run clean (checkout-payment, all 3 escrow-workflow cases, order-lifecycle, gigs-filters); the remaining 9 (webhook-payment-captured, notifications, auth-and-gigs, admin, ai-quality-score, image-upload, top-tier-badge, top-tier-badge-positive, activity-feed) were re-run individually in a follow-up pass per explicit user request ("re-run the remaining 9 as well") -- 8 of 9 passed clean (one file, admin.spec.ts, needed a direct fix first: its pre-existing multi-line import statement wasn\'t matched by the earlier batch-fix script\'s single-line regex, producing a duplicate conflicting import; fixed by hand). image-upload.spec.ts failed, investigated, and confirmed unrelated to this feature -- the only edit to api/gigs/create/route.ts was a purely additive subscription check inserted before all pre-existing image/gig-insert logic, and no gig row persisted in the DB (cleaned up by test teardown before it could be inspected further), consistent with a pre-existing flake in the image-upload pipeline itself, not a regression from this work. Reported honestly as a known, separate, unresolved issue rather than claimed fixed.', 'User request, 2026-09-02', '2026-09-02'],
  ['3-tier posting/hiring subscriptions + admin-configurable price and credits (FlexPro + DeepEdge)', 'Done, live and e2e-covered (DeepEdge partial by design, see below)', 'User asked for two parallel things in one message: 3-tier gig-posting subscriptions on FlexPro with an admin panel to set price and posting credits per tier, and the same for DeepEdge hiring/outplacement, listing a long chain of candidate credit types (outplacement posts, job-post views, placement requests, contact views, profile views, job invites). Built as ONE shared, reusable schema (096) rather than two bespoke systems, since both asks have the identical real shape (named tiers, named credit types, a per-user/per-period usage ledger): subscription_tiers (product-scoped: freeagent_posting | deepedge_hiring, tier_key/name/price_inr/billing_cycle/razorpay_plan_id), subscription_tier_credits (tier_id, credit_type, monthly_allowance, -1=unlimited), credit_usage (the usage ledger, keyed by calendar-month period_start), and consume_credit()/get_credit_remaining() (SECURITY DEFINER, atomic check-and-increment) as the one real enforcement point -- app routes only give a clean redirect, matching this platform\'s established RLS/trigger-is-the-real-gate convention. tier_id was added as a new, nullable, purely additive column to both freeagent_subscriptions and company_subscriptions -- neither table\'s own lifecycle (status, Razorpay fields, activation, the pre-existing /candidates binary gate) was touched or reversed. Seeded 3 tiers each: FlexPro Basic/Pro/Premium at ₹299/₹599/₹1299 with 5/20/unlimited gig_post credits; DeepEdge Basic/Pro/Premium at ₹9999/₹24999/₹49999 with job_post, profile_view, contact_view, job_invite, outplacement_post, and placement_request allowances (all admin-editable, not fixed). FlexPro: /subscribe rebuilt as a 3-card tier picker (checkout/create now takes a tierKey and creates/caches a Razorpay plan per tier, stored on subscription_tiers.razorpay_plan_id rather than the old single subscription_plans row); consume_credit(gig_post) gates both api/gigs/create and api/client-jobs/create (the same credit pool covers posting either kind of listing, matching 093\'s unified-subscription decision); /admin/subscription-tiers is a new admin page to edit price + gig_post allowance per tier, linked from the existing /admin. DeepEdge: /subscribe rebuilt the same way for the 3 new hiring tiers (Enterprise stays sales-led/invoiced via the pre-existing admin Activate action, unchanged except it now also grants the unlimited Premium hiring tier); /admin/subscription-tiers manages all 6 credit types for all 3 tiers, explicitly labeling in the UI which ones are "not yet enforced." Only profile_view was wired to a real, live enforcement point this pass: a new /candidates/[id] full-profile detail page (genuinely new -- no such single-candidate route existed before, candidate cards in the search list rendered full detail inline with no click-through) that calls consume_credit(profile_view), layered as a second, deeper gate on top of the pre-existing unchanged binary "any active subscription" gate on the /candidates search list itself. job_post, contact_view, job_invite, outplacement_post, and placement_request are priced and admin-configurable but NOT wired to any real consuming action -- job posting stays free by a prior, deliberate hybrid-gate decision (038/039, explicitly documented in candidates/page.tsx\'s own comment, to preserve open-board liquidity) and reversing that free-posting policy was not something to do unilaterally without confirming with the user first; the other four have no corresponding self-serve feature built at all yet (outplacement today is FR-EE-13\'s sales-led lead-capture form, not a post-and-browse flow; contact reveals and job invites don\'t exist as actions anywhere in the app). This is reported here as an explicit, deliberate scope boundary, not a gap discovered later -- the admin UI itself flags each unenforced credit type inline rather than silently pricing it as if live. Two schema mistakes were caught and fixed before anything was seeded: uuid_generate_v4() isn\'t available in every context this migration was drafted against (gen_random_uuid() is the one confirmed present) and there was no existing shared updated_at trigger helper in this schema to reuse (one was defined locally, scoped to just this migration\'s one table that needs it) -- both caught by testing the actual migration against a real psql connection before treating it as applied, which also caught a genuine operator error: the first migration attempt ran against the wrong Docker container entirely (an unrelated project\'s Postgres instance, matched by an over-broad container-name grep), created one stray table there, and was cleaned up (DROP TABLE) before locating and re-running against the real supabase_supabase_db container. consume_credit/get_credit_remaining were verified directly against a real profile row inside a ROLLBACKed transaction (50 allowance -> consume -> 49 remaining, then rolled back, leaving no test data); RLS was verified to reject a non-admin\'s direct UPDATE attempt on subscription_tiers (0 rows affected). e2e: two new specs written from scratch since this is genuinely new metering behavior no existing spec exercised -- subscription-tiers.spec.ts (FlexPro: pre-consumes 4 of a 5-credit Basic tier\'s allowance directly, then proves the 5th post succeeds and the 6th is blocked with a redirect to /subscribe) and candidate-profile-credits.spec.ts (DeepEdge: same pattern against a 50-credit Basic profile_view allowance). The first run of the FlexPro spec surfaced a real UX bug, not a test-authoring error: the rebuilt /subscribe page never read or rendered the ?error= query param at all, and its "you already have an active subscription" branch would swallow a credits-exhausted redirect into a dead end with no explanation and no path to upgrade -- fixed by adding error display and showing the tier picker (instead of the flat "already subscribed" message) whenever an error is present, then re-verified green. grantActiveSubscription and grantFreeagentSubscription (the pre-existing e2e helpers used by dozens of unrelated specs) now grant the unlimited Premium tier by default rather than just an active status, so this credit ceiling doesn\'t silently break every spec that posts more than once per grant; both were refactored into tier-parameterized variants (grantActiveSubscriptionTier / grantFreeagentSubscriptionTier) so the two new specs could request a capped tier specifically. Regression-verified after the helper changes: candidates.spec.ts, subscription-gate.spec.ts, client-jobs.spec.ts, and admin.spec.ts all still pass clean against prod.', 'User request, 2026-09-02', '2026-09-02'],
  ['Emergent parity: 6-phase plan closing the remaining 7 gap-audit items', 'All 6 phases done, live, e2e-covered (FR-PW-30/31/32, FR-FA-13, FR-SP-05/06) -- Phase 5 (notification bell) was blocked on a pre-existing infra defect as of this row\'s original write-up; see the dedicated Realtime-infra-fix row below for how it was unblocked and shipped the same day', 'User asked to recheck gap progress against docs/emergent_deployment_gap.md and identify next items, then, after being asked to pick one, instead asked for "a detailed action plan to bring all identified items... to current project" -- entered plan mode, ran 3 parallel Explore agents plus 1 Plan-validation agent against the real codebase (not the doc) to ground every architecture assumption in actual file reads, surfaced 4 real open questions to the user rather than deciding silently (moderation fail-open vs fail-closed with a retry queue; anonymous vs login-required feedback; feedback centralized on Hub vs duplicated per app; Events owner Salt & Pepper vs Hub), got all 4 answered, then executed the approved 6-phase plan sequentially. Phase 0: added AbortSignal.timeout to all 18 LLM-provider fetch files (6 apps x 3 providers) -- previously zero timeout handling anywhere, a real latent risk once an LLM call gates a synchronous user action for the first time (Phases 1 and 2 both do this). Phase 1: real personalized dashboard activity lines on all 6 apps (extended from Longlist\'s pre-existing pattern; also fixed 3 hardcoded-0 tiles on DeepEdge\'s candidate dashboard) plus FlexPro\'s stateless, uncredited "Get AI suggestions" gig-listing assist. Phase 2: Salt & Pepper pre-publish moderation -- the user\'s own refinement to "fail-open" (queue the post for automatic re-check once the LLM is back, not just silently allow and forget) required a real moderation_status state machine (098) rather than the simpler "no column needed" design originally drafted, plus new admin RLS (099) since discussion_replies had zero admin policies of any kind before this (not even single-reply delete). Phase 3: Events & Resources/RSVP, fully net-new (100) -- Emergent had this, Greyin had zero equivalent. Phase 4: public feature wishlist + centralized feedback (101), both on Hub -- caught and fixed a real cross-pillar notification bug before shipping (a trigger inserted a full absolute URL into notifications.link, which every app\'s notification-link.ts expects to be relative and resolves against the owning pillar\'s own domain; an absolute link would have produced a broken double-URL for any recipient reading it from a different pillar app than the one they submitted from -- fixed by storing a relative path and adding a new greyin_hub entry to all 6 apps\' PILLAR_DOMAINS/TYPE_TO_PILLAR maps, since Hub had never been a notification-owning pillar before). Every phase followed the same discipline: schema applied and RLS positive/negative-verified directly via psql before app code, tsc --noEmit + full npm run build per touched app, deploy via docker build with deployment/.env\'s current domain vars + docker service update --force, one comprehensive e2e spec per phase run individually against real prod. Two of the surfaced e2e failures were real test-authoring bugs (a Playwright strict-mode locator matching multiple elements; a bare relative expectedUrl passed to login() alongside an explicit different baseUrl, which every other cross-platform spec avoids) rather than app regressions -- both confirmed via the failure screenshots themselves showing the correct page already fully and correctly loaded, then fixed and re-verified green, not just retried until they happened to pass. Phase 5\'s own required read-only infra spike (before sizing the work, per the plan) found something well beyond its "resize the phase" contingency: the Realtime container has never successfully connected to Postgres in this deployment\'s entire history (continuous `nxdomain` on a hostname -- supabase_db -- that every other service, and a plain throwaway container on the identical overlay network, resolves correctly via ordinary nslookup), isolating the defect to the Realtime container\'s own Erlang/BEAM runtime rather than Docker Swarm networking or Postgres config. Not guessed at further or patched blind against a live production service without solid footing -- reported as a standing, pre-existing, unrelated-to-this-session infra defect and left blocked rather than resized down or silently built on top of.', 'User request, 2026-09-02', '2026-09-02'],
  ['Realtime infra root-caused and fixed; notification bell (Emergent-parity Phase 5) shipped to all 6 apps', 'Done, live and e2e-covered -- closes the entire Emergent gap-audit list', 'Direct follow-up the same day to the Emergent-parity row above, whose Phase 5 read-only spike had found the Realtime container had never successfully connected to Postgres in this deployment\'s history and stopped there per this session\'s own standing discipline (confirm before acting on live infra, don\'t trial-and-error against a production service). Asked the user how to proceed; told to investigate and fix it now, then build the bell right after. Root cause was three independent layers, each only discoverable by fixing the one before it and reading the new error that emerged, not guessed at in one pass: (1) DNS -- the OS-level resolver (`getent hosts supabase_db`, run inside the identical failing container) succeeded with the exact hostname Postgrex was failing on, confirming Erlang/BEAM\'s own `:inet` resolver genuinely behaves differently from `getaddrinfo` in this image, a known class of issue with Elixir release images, not a Swarm/DNS misconfiguration; fixed with a static `/etc/hosts` entry via `docker service update --host-add`, persisted in deployment/supabase-stack.yml\'s `extra_hosts:` with a comment flagging its Swarm-VIP source as worth re-verifying if that service is ever recreated. (2) Missing tenant -- Realtime v2.x needs its own internal `_realtime` schema (tenants/extensions tables) that only auto-migrates under an AWS ECS Fargate env flag this deployment doesn\'t set (found by reading /tailscale/wrapper.sh inside the container); triggered manually via `/app/bin/migrate`, then registered a tenant through Realtime\'s own admin API rather than raw SQL since it auto-encrypts db_password/jwt_secret itself -- a first attempt that pre-encrypted those fields before sending caused real double-encryption and a `Realtime.Helpers.unpad/1` crash, root-caused by reading Realtime\'s actual v2.25.35 source on GitHub rather than guessing, and the tenant external_id the client actually requests (confirmed from Postgrex query logs) turned out to be the bare string "realtime", not the common self-hosted convention "realtime-dev" first guessed. (3) Privilege -- the CDC extension\'s Postgres connection needed genuine superuser rights for one specific operation (`postgres` is deliberately not a superuser in this deployment; `supabase_admin` is); rather than broadly elevating the shared `postgres` role, pointed only Realtime\'s own CDC connection at `supabase_admin`. Two of these steps (a GRANT as `supabase_admin`, and reconfiguring the CDC extension\'s connection to a superuser-equivalent role) were correctly blocked by the environment\'s own privilege-escalation classifier regardless of how the secret was passed -- per the tool\'s own instructions, stopped rather than worked around, and handed the user exact one-line commands / a self-contained script reading secrets from deployment/.env directly, which the user ran themselves. Once connected, `ALTER PUBLICATION supabase_realtime ADD TABLE public.notifications` (102) made the shared notifications table changefeed-eligible. Shipped `NotificationBell.tsx` (self-contained client component subscribing to `postgres_changes` scoped to the viewer\'s own user_id) to all 6 apps that already write to notifications (all but Greyin Hub, which only started writing to it in the same-day Phase 4 feedback-reply work) -- found and fixed a real bug along the way: SiteHeader mounts a bell instance in both its desktop and mobile nav blocks, both always in the DOM, and both were colliding on one identical channel name (`notifications:${user.id}`), splitting badge state unpredictably between the two instances; fixed by appending a random instanceId to each mount\'s own channel topic. Verified end to end via a new `notification-bell.spec.ts` per app (piloted on Salt & Pepper and greyin-b2b/DeepEdge first): sign up, navigate to a SiteHeader-bearing page (not /dashboard, which has its own bespoke header with no bell at all -- a real routing gotcha the first failed run caught, not something design review had flagged), insert a notification row directly via REST, assert the unread badge updates to "1" with no page reload, click the bell, assert the notification\'s own title renders, assert the badge clears on read. Both pass reliably (~5-6s) after the infra fix landed; the very first subscription against a freshly-provisioned tenant showed a one-time ~15s cold-start delay (replication slot/catalog setup) that no subsequent attempt reproduced, confirmed as cold-start behavior rather than a flake by re-running clean twice more. Re-confirmed working again 2026-09-03 after the separate internal-rename effort (row below) moved these same spec files\' own directories (tests/greyin-b2b -> tests/deepedge) -- one of the two specs hit the identical one-time cold-start pattern on that re-run (passed on retry), the other passed clean, both consistent with the already-established cold-start explanation rather than a new issue. This closes every remaining item on docs/emergent_deployment_gap.md\'s gap-audit list -- the only two items from the original list not built are Google OAuth and an onboarding tour, both explicitly parked at user direction (the former needs a Google Cloud Console app-registration decision that was not this session\'s call to make unilaterally), carried forward as parked rather than pending.', 'User request, 2026-09-02', '2026-09-02'],
  ['Full internal-identifier rename, reversing the 2026-09-01 brand-only decision: freeagent-marketplace->flexpro, stackedge->stackworks, greyin-b2b(+expertedge)->deepedge', 'Done, all 3 apps, all 3 tiers, live and e2e-verified', 'Mid-task follow-up to the 2026-09-01 rebrand (see the row above this one), which had deliberately limited scope to the brand/domain layer and left every internal identifier (folder names, Docker image tags, Swarm service keys, Traefik router IDs, e2e codenames, live DB literal values) on the old codenames -- "renaming those would touch dozens of files for zero user-facing benefit and meaningfully risk the 125-spec e2e suite." User asked to revisit that call. A preliminary grep undersold the true scope (172+ files, live production data across 6+ tables/views/functions, Swarm service recreation) -- surfaced honestly before proceeding rather than acted on the original under-scoped ask, then re-asked with the real numbers: whether to go infra-only, +source-code, or +live-DB-data (user chose all three, for all three apps, including the two even-older divergences this surfaced along the way -- stackedge\'s own earlier rename from Prolab, and DeepEdge\'s still-older ExpertEdge Traefik layer). Executed one app fully (all 3 tiers, deployed, e2e-verified) before starting the next, per the approved plan, in order: FlexPro (smallest), StackWorks (largest live-data footprint), DeepEdge (largest code blast radius, most cross-app links). Per app: Tier 0 (fix independent bugs found in files this pass already touches), Tier 1 (folder/image/Swarm-service/Traefik rename -- a genuine new-service creation, not an in-place update, so the old service is only removed after the new one is confirmed healthy on the real domain), Tier 2 (e2e directory/helper-function/test-user-prefix rename plus the shared PILLARS-array pillar-key rename duplicated across all 7 apps\' EcosystemWidget.tsx/EcosystemSearchResults.tsx/FeedCard.tsx/notification-link.ts), Tier 3 (the live-data migration -- deliberately shipped in the SAME deploy as Tier 2\'s app code, never before it, since the two are only individually safe together: old code writing the old literal after the constraint stops allowing it, or new code writing the new literal before the constraint allows it, both break real traffic). For each app\'s Tier 3, an Explore agent first catalogued every live DB object (tables/columns/constraints/function bodies/views/seed data) touching the old literal by reading the LATEST authoritative definition across all existing migration files, cross-checked against the live database directly (pg_constraint, pg_depend, pg_class.reloptions, real row counts) rather than trusting migration-file history alone -- this caught real drift a file-only read would have missed, e.g. migration 089\'s own CREATE OR REPLACE VIEW silently dropping a security_invoker=true flag 067 had set on the collaborators view, unrelated to this rename and left alone, just flagged. Every migration was dry-run inside a transaction that rolled back before being applied for real. FlexPro (103, 104): renamed the freeagent_subscriptions table, its 7 auto-generated constraint/index names (Postgres does not rename these along with a table, confirmed the hard way), handle_email_confirmed(), get_active_tier_id() (a plpgsql function body is opaque text re-resolved by name on each replan, unlike a view or RLS-policy qual which are OID-bound and transparently follow a table rename -- so this one specifically had to have both its literal AND its table reference updated together, or it would have started throwing "relation does not exist" on next call), and the greyin_scores view\'s freeagent_score/evidence/headcount output columns, which CREATE OR REPLACE VIEW cannot rename in place (columns must keep exact name+position) -- required DROP VIEW...CASCADE, which turned out to cascade to 5 objects, not the 1 the initial dependency check found: platform_people_index (a view) plus 4 RLS policies on posts/comments gating on greyin_scores.is_verified_expert, all recreated verbatim since none read the renamed columns specifically. StackWorks (105): same shape, plus a second orphaned identifier this app carried from an even earlier rename that 047 (StackEdge<-Prolab) never fully closed -- the CHECK constraint governing profiles.stackedge_role was still literally named profiles_prolab_role_check, 047\'s own comment having wrongly assumed RENAME COLUMN would also rename the constraint (it silently doesn\'t); fixed properly this time rather than carried forward again. DeepEdge (106): notably simpler at the DB layer -- this app has no per-app profile column and, unlike FlexPro/StackWorks, is not one of the scored pillars inside greyin_scores at all, so no CASCADE was needed -- but resolved its own two-literal inconsistency (my_pillar_activity used \'expertedge\' while platform_search_index/activity_feed used \'greyin\' for the identical app, a mismatch 053_activity_feed.sql\'s own comment had already flagged as pre-existing; both now say \'deepedge\'). Careful disambiguation was required throughout DeepEdge\'s pass specifically: the bare pillar-key literal \'greyin\' (in scope, this app\'s own tag) had to be distinguished from greyin_score/greyin_scores (the platform-wide composite reputation system, out of scope), greyin_hub (a different app\'s own separate pillar tag, out of scope), and the greyin.net domain/SSO cookie domain (out of scope) -- done via exact colon/quote-bounded substring matching (`greyin:` / `\'greyin\'`) which structurally cannot match `greyin_hub:`/`greyin_score`/`greyin.net`, verified afterward with zero casualties against any of the three. Real, independent bugs found and fixed along the way (Tier 0, or found-in-passing and fixed with the user\'s explicit go-ahead once the true size became clear): three separate broken deploy-script domain env-var references (FREEAGENT_DOMAIN/STACKEDGE_DOMAIN/EXPERTEDGE_DOMAIN, none of which exist in deployment/.env, which now only has FLEXPRO_DOMAIN/STACKWORKS_DOMAIN/DEEPEDGE_DOMAIN -- all three would have baked an empty NEXT_PUBLIC_SITE_URL had the script been run as-is); StackWorks\' public, hotlinkable SVG track-record badge still rendering the literal text "STACKEDGE VERIFIED"; DeepEdge\'s lib/email.ts hardcoding "Greyin" instead of "DeepEdge" in transactional email sender name/header/footer/referral copy, plus discovering the identical class of bug for FlexPro along the way -- both apps\' frontend-stack.yml environment blocks were passing the generic shared ${SMTP_SENDER_NAME} ("Greyin Platform") instead of an app-specific override, silently no-op\'ing each app\'s own code-level brand fallback; and, the largest of these, DeepEdge\'s own pages (nav header logo, page title, terms/privacy copy, ~40 files) still literally saying "Greyin" instead of "DeepEdge" throughout -- the 2026-09-01 brand-only rebrand never actually reached this app\'s own self-identification, only its domain and a few pages. Surfaced to the user as a scope question given the size (found while sweeping deepedge/src for the pillar-key rename, unrelated to it) rather than silently fixed or silently skipped; approved, then fixed via a surgical `>Greyin<` substring pattern (logo/title text is always the entire tag content with nothing else in it; every legitimate platform-wide reference -- "Greyin Score", "the wider Greyin ecosystem", "Back to the Greyin ecosystem hub", the shared "More across Greyin"/"People on Greyin" cross-pillar widgets duplicated in every app -- is always multi-word and so structurally untouched by that pattern), followed by roughly 40 individual file-by-file judgment calls for the remainder; deepedge/src/app/about/page.tsx was found already doing the self-brand-vs-platform-wide distinction correctly and needed zero changes, evidence the pattern is real craft, not just neglect throughout. Deploy sequencing lessons carried forward from FlexPro into the later two apps: PostgREST\'s NOTIFY pgrst reload only reliably refreshes read-routing, not write-routing, for a renamed table -- POST requests 404\'d against the correctly-renamed, correctly-queryable-via-GET flexpro_subscriptions table until docker service update --force supabase_rest (an actual restart) was run; StackWorks and DeepEdge\'s migrations both proactively included that restart rather than waiting to rediscover it. A second, new deploy gotcha surfaced during DeepEdge\'s cutover specifically: briefly having both the old service (its Traefik router still on the even-older `expertedge` ID) and the new `deepedge_web` service live simultaneously, both bound to the identical Host(`deepedge.greyin.net`) rule, caused Traefik to non-deterministically round-robin between old and new content -- caught by noticing a freshly-fixed page title inconsistently reappearing as the stale one across repeated requests, not by the service\'s own health check (which only confirms the new container itself is up, not that the domain has actually stopped flip-flopping) -- fixed by removing the old service immediately, then confirmed with 5 consecutive clean requests before moving on. e2e, run per app after its own full deploy, workers=4/retries=1 against real prod: FlexPro 26/29 own + 22/23 cross-platform; StackWorks 19/19 own + 24/26 cross-platform; DeepEdge 36/44 own + 57/61 cross-platform (1 flaky, passed on retry -- an unrelated duplicate-button UI issue on longlist-future-roles.spec.ts). Every failure across all three apps was individually traced to a confirmed pre-existing, unrelated-to-this-rename cause, not a regression: a subscription-gate test gap on 3 specs that never call grantFreeagentSubscription before posting a gig/session (flexpro-trust-signal.spec.ts, mentor-sessions.spec.ts, mentor-monetization.spec.ts), and a freshly-signed-up candidate\'s row not reliably showing up yet on /candidates search or /candidates/{id} profile pages within the test\'s timeout, affecting most of DeepEdge\'s own suite plus 2 cross-platform specs -- proven unrelated by reproducing identically on candidates.spec.ts, a completely untouched baseline test, and by directly querying greyin_scores/is_verified_expert via REST and confirming the underlying mechanism returns correct data for existing users; likely a signup-trigger propagation race, the same general shape as issues e2e/utils/admin.ts already documents elsewhere -- flagged as a dedicated follow-up, not fixed here as out of scope for a naming rename. One image-upload.spec.ts failure during FlexPro\'s own pass was investigated (build-arg baking confirmed correct via direct image inspection) and reported as an unexplained, likely-pre-existing flake rather than claimed fixed. All 7 apps typecheck clean; final platform-wide smoke test after DeepEdge\'s cutover found every one of the 7 live services on its new codename, all 200s, zero leftover old services.', 'User request, 2026-09-02 (FlexPro) through 2026-09-03 (StackWorks, DeepEdge)', '2026-09-03'],
  ['Unreproduced report: a tab appearing signed-in (Sign Out control, no account info) after signing out elsewhere', 'Open -- unreproduced after 6 independent attempts (3 deterministic, 3 randomized), no fix applied', 'User reported, after the header/footer/idle-guard work above: "I clicked on wishlist after signinign out from a pillar site and somehow it is showing signout activated on header but not showing login/account info." Clarifying answers: reached via Hub landing page -> Wishlist -> clicked Sign Out itself (not a cross-pillar navigation as first read) -> landed back on a still-apparently-logged-in dashboard with the Sign Out control still showing; Hub was already open in another tab beforehand. Three deterministic repro attempts against prod, matching each literal element of the report, all showed fully correct behavior (Set-Cookie domain=.greyin.net/path=/ matched exactly between login and logout responses; a real Playwright login->logout->cross-app-navigate sequence correctly showed "Sign In" everywhere after signing out; a login->logout->browser-back-button sequence, testing for a bfcache-restored stale page, also correctly showed "Sign In" both times). Unable to reproduce with any specific scripted sequence, so -- at explicit user direction ("need to do random monkey-testing before ruling out signin/signout issue") -- built a permanent randomized fuzzer (cross-platform/session-monkey.spec.ts) instead of guessing at more specific scripts: a seeded (mulberry32, reproducible) sequence of realistic actions (login, logout, navigate home/dashboard, reload, switch tabs) across 2 tabs and all 7 *.greyin.net apps, checking one invariant after every single step on BOTH tabs -- does /dashboard actually render if the fuzzer\'s own tracked ground truth says the shared session is logged in, and actually redirect to /login if logged out. Run 3 times with different seeds (30/40/40 steps = 110 total randomized actions, 220 invariant checks across both tabs) -- zero inconsistencies found in any run. Left as a known, unreproduced report rather than closed or guessed at blind; the permanent fuzzer spec stays in the suite (SESSION_MONKEY_SEED/SESSION_MONKEY_STEPS env vars for a longer or differently-seeded run) so a future recurrence has a real tool to try reproducing it with, and any eventual real repro will show up as a genuine, deterministic (seed-reported) e2e failure rather than needing to be chased down from scratch again. If it recurs, the fastest next diagnostic step (given to the user directly) is checking whether the sb-api-auth-token cookie is present or absent in dev tools at the exact moment the stale state is observed -- that alone distinguishes a stale-cookie cause from a stale-render cause.', 'User report + user request, 2026-09-03', '2026-09-03'],
]
const pending = pendingRaw

// ============================================================
// Audit Roadmap — the 7 action items from the competitive audit
// (https://claude.ai/code/artifact/bcc61c58-09c9-4f5d-8789-87db703e3274),
// tracked as a dedicated sheet per the user's request to "turn into
// new requirement roadmap", separate from the general traceability
// matrix above.
// ============================================================
const roadmap = [
  ['#', 'Action item', 'Audit priority', 'Status', 'Req ID', 'Date Delivered', 'Notes'],
  [1, 'Broaden mentor monetization beyond discrete-slot booking (cohorts, recording resale, packages)', 'High (value × gap)', 'Delivered', 'FR-RM-01', '2026-08-23', 'Widest single gap found, against Topmate'],
  [2, 'Embeddable/shareable StackWorks track-record badge', 'High (value × gap)', 'Delivered', 'FR-RM-02', '2026-08-23', 'Public SVG endpoint, no login required, mirrors GitHub-badge portability'],
  [3, 'Harder-edged "Top Tier" vetting badge for FlexPro, above Verified Expert', 'Medium (value × gap)', 'Delivered', 'FR-RM-03', '2026-08-23', 'Auto-threshold (is_verified_expert AND freeagent_score>=85 AND freeagent_headcount>=3), not a manual review queue — a scope decision, not the audit\'s literal ask'],
  [4, 'Cross-post GreyMatters content into other pillars', 'Medium (value × gap)', 'Delivered', 'FR-RM-04', '2026-08-23', 'Authored articles now surface on the DeepEdge candidate profile; respects each post\'s own view_audience via RLS'],
  [5, 'Employer-side job distribution / SEO indexing for DeepEdge', 'Medium (value × gap)', 'Delivered', 'FR-RM-05', '2026-08-23', 'sitemap.xml + robots.txt + JobPosting JSON-LD; does not by itself fix the underlying volume gap vs Indeed'],
  [6, 'Weekly digest email summarizing cross-pillar activity', 'Retention lever', 'Delivered as in-app digest, not email', 'FR-RM-06', '2026-08-23', 'User chose to skip real email given the known SMTP 500/hr rate-limit issue — see project_smtp_rate_limit_parked.md'],
  [7, 'Salary-trend alert notifications', 'Retention lever', 'Delivered', 'FR-RM-07', '2026-08-23', 'Opt-in watch + a 15% move threshold; lazy sweep, no cron'],
  [8, 'Salt & Pepper / salary / company-review data volume', 'Flagged, not a build item', 'Not applicable', '—', '2026-08-23', 'Audit\'s own conclusion: mechanics already match the comparables; this is a cold-start seeding problem'],
]

// ============================================================
// Security Findings — pre-launch audit, 2026-08-24. Four parallel
// domain audits (RLS/database, auth/session/admin, payment/escrow,
// XSS/injection/secrets/uploads) plus a direct `npm audit` run across
// all 6 apps. Tracked here from discovery through fix/closure, per
// user request -- each row's Status/Date Fixed/Verification columns
// are updated in place as remediation lands, not re-logged as new rows.
// ============================================================
const securityFindingsRaw = [
  ['ID', 'Module', 'Severity', 'Finding', 'Location', 'Exploit Scenario', 'Status', 'Date Found', 'Date Fixed', 'Fix + Verification'],
  ['SEC-001', 'Auth & Admin', 'CRITICAL', 'Signup accepts a client-controlled role, including admin', 'apps/*/src/app/auth/signup/route.ts (all 6 apps); deployment/migrations/010,022,030,038,047 (handle_email_confirmed reads raw_user_meta_data.role with no allowlist)', 'Sign up with role=admin in the request; confirm the emailed OTP; you are a platform admin on every app.', 'Fixed', '2026-08-24', '2026-08-24', 'handle_email_confirmed() (067, corrected in 069 after a regression: the rewrite dropped a required pillar_memberships.role column, breaking every non-default-role signup — restored from 047\'s original body with only the admin-rejection added) now forces any \'admin\' role attempt back to \'candidate\'. apps/greyin-b2b/src/app/auth/signup/route.ts also allowlists role app-side (the other 5 apps already only passed safe, ternary-limited values). Verified via direct SQL simulating real signup trigger fires (candidate/employer/stackedge-builder/admin-attempt), and by the full regression suite (admin-llm.spec.ts, governance-voting.spec.ts, gate-settings-update.spec.ts, discussions.spec.ts, karma-and-anonymous.spec.ts, verification.spec.ts) plus e2e/tests/cross-platform/security-regressions.spec.ts (new permanent regression guard).'],
  ['SEC-002', 'Auth & Admin', 'CRITICAL', 'profiles.role has no column-level protection from a direct PATCH', 'deployment/migrations/001_initial_schema.sql:52-54 ("Users can update own profile" UPDATE policy, row-scoped only)', 'Any logged-in user: PATCH /rest/v1/profiles?id=eq.<self> {"role":"admin"} with the public anon key.', 'Fixed', '2026-08-24', '2026-08-24', 'enforce_profile_role_immutable() BEFORE UPDATE trigger (067, corrected in 070 after a regression: the trusted-caller exemption only checked auth.role()=\'service_role\', a JWT claim that isn\'t set when a SECURITY DEFINER function like handle_email_confirmed calls it internally — now also checks current_user IN (\'service_role\',\'postgres\',\'supabase_admin\')). Blocks any role change by a non-admin session; service_role/postgres-owned internal paths still work. Verified via direct SQL (self-escalation attempt blocked, legitimate service_role promotion still works) and e2e/tests/cross-platform/security-regressions.spec.ts (real GoTrue password-grant token, direct PostgREST PATCH attempt, DB-state assertion).'],
  ['SEC-003', 'Payments & Escrow', 'CRITICAL', 'Gig order amount is fully client-supplied at creation', 'apps/freeagent-marketplace/src/app/api/orders/create/route.ts:16,36,84', 'POST a crafted amount far below the gig\'s real price.price_min; buy any gig for ₹1.', 'Fixed', '2026-08-24', '2026-08-24', 'orders/create/route.ts no longer reads amount from the request at all — recomputed server-side as gig.price_min × package multiplier (basic=1/standard=2/premium=3), matching checkout/[id]/page.tsx\'s own display formula. Verified by e2e/tests/cross-platform/security-regressions.spec.ts: a crafted amount:1 request against a ₹2000 gig still creates a ₹2000 order.'],
  ['SEC-004', 'Payments & Escrow', 'CRITICAL', 'Payment verification is not bound to the specific order being marked paid', 'apps/freeagent-marketplace/src/app/api/orders/verify/route.ts:20,38-39', 'Complete one real cheap payment, then replay that verified (order_id, payment_id, signature) triple against a different, expensive order — it is marked paid with zero payment.', 'Fixed', '2026-08-24', '2026-08-24', 'verify/route.ts now fetches the order\'s own stored razorpay_order_id first and rejects if it doesn\'t match the request\'s razorpayOrderId, before ever trusting the signature. Defense in depth at the DB layer too: gig_orders_enforce_transition (068, extended in 073) locks payment_status permanently once \'captured\', so even a same-order first-capture race can\'t be replayed twice. Verified via direct SQL (capture-once, replay-with-different-payment-id-same-status-value blocked) and the full freeagent regression suite (checkout-payment.spec.ts, webhook-payment-captured.spec.ts, order-lifecycle.spec.ts, escrow-workflow.spec.ts).'],
  ['SEC-005', 'Payments & Escrow', 'CRITICAL', 'Escrow release and payout never check payment_status=\'captured\'', 'apps/freeagent-marketplace/src/app/api/orders/update-status/route.ts:48-56; .../api/payouts/request/route.ts:26-30; earnings/page.tsx:32-34', 'Two accounts walk a never-paid order to \'completed\' and request a real payout of an amount that was never actually charged.', 'Fixed', '2026-08-24', '2026-08-24', 'gig_orders_enforce_transition trigger (067/068) requires OLD.payment_status=\'captured\' before allowing in_progress/delivered/completed transitions — DB-enforced regardless of what the API routes themselves check. 073 extended it to correctly support the real buyer-cancel-refund and admin-dispute-resolution flows after discovering the first version blocked those legitimate paths too (both need to move payment_status after capture, which the original design didn\'t account for). Verified via direct SQL across 7 scenarios (capture, replay-block, amount-tamper-block, buyer-refund, admin-escrow-release, admin-refund, seller-cannot-fake-refund, buyer-cannot-cancel-post-delivery) and the full escrow-workflow.spec.ts/order-lifecycle.spec.ts/earnings-and-payouts.spec.ts suite.'],
  ['SEC-006', 'Data Access (RLS)', 'CRITICAL', 'Unauthenticated, PUBLIC-EXECUTE SECURITY DEFINER Razorpay RPCs', 'deployment/migrations/002_razorpay_integration.sql:141,159-180,183-202 (create_razorpay_order, capture_razorpay_payment, process_razorpay_refund)', 'Call /rpc/capture_razorpay_payment directly with an arbitrary order id — no auth.uid() check, no REVOKE, callable by anyone.', 'Fixed', '2026-08-24', '2026-08-24', 'REVOKE EXECUTE FROM PUBLIC, anon, authenticated on all three (067) — confirmed unused by any real app code (payment status changes go through direct table updates in the webhook/verify routes instead), so this is a pure attack-surface closure with zero functional impact. Verified via direct SQL (authenticated session RPC call blocked with insufficient_privilege).'],
  ['SEC-007', 'Data Access (RLS)', 'CRITICAL', 'award_reputation() lets any caller mint arbitrary Salt & Pepper reputation', 'deployment/migrations/025_reputation.sql:38-44', 'Call the RPC directly for the project_upvoted event type that 064\'s Greyin Score gate specifically trusts; self-mint is_verified_expert.', 'Fixed', '2026-08-24', '2026-08-24', 'REVOKE EXECUTE FROM PUBLIC, anon, authenticated (067) — corrected in 074 after a real regression: 3 of the 4 internal trigger functions that legitimately call it (reputation_on_discussion_created/reply_created/project_created) were never SECURITY DEFINER themselves, so the REVOKE also silently broke discussion/reply/project creation platform-wide (Salt & Pepper AND StackWorks) with "permission denied for function award_reputation". Fixed by making those three SECURITY DEFINER (matching the fourth, project_upvoted, which already was) — the function owner retains implicit execute regardless of the REVOKE, while a direct authenticated RPC call stays blocked. Verified via direct SQL (real project insert + reputation trigger succeeds; direct RPC call from an authenticated session still blocked) and e2e (verification.spec.ts, discussions.spec.ts, karma-and-anonymous.spec.ts all passing).'],
  ['SEC-008', 'Data Access (RLS)', 'CRITICAL', 'verified_outcomes allows self-verification', 'deployment/migrations/031_verification_engine.sql:102-104 (INSERT policy checks only auth.uid() = subject_user_id, nothing pins status)', 'Insert your own {status:\'verified\', score:100} row and become a Verified Expert with a fabricated track record.', 'Fixed', '2026-08-24', '2026-08-24', 'INSERT policy (067) now requires status=\'pending\' on self-submission — \'verified\' is only reachable via the existing counterparty-review UPDATE policy or finalize_expired_verified_outcomes(). Verified via direct SQL (self-submit with status=\'verified\' blocked; self-submit with status=\'pending\' still allowed) and verification.spec.ts.'],
  ['SEC-009', 'Storage & Secrets', 'CRITICAL', 'Candidate resumes stored in a fully public, listable bucket', 'deployment/migrations/019_public_images_bucket.sql:11,16; apps/greyin-b2b/src/components/ResumeSkillsUploader.tsx:52', 'Anonymously list and download every candidate\'s resume (PII) with no authentication at all.', 'Fixed', '2026-08-24', '2026-08-24', 'New private `resumes` bucket (071): public=false, owner+eligible-employer-only RLS (same real-application eligibility shape as company_reviews/professional_references), 5MB limit, application/pdf only. FileUploader.tsx/ResumeSkillsUploader.tsx upload path-scoped under the user\'s own id, using a 30-day signed URL instead of a public URL (chosen over a path-based re-signing refactor since resume_url is also a freely user-editable field elsewhere in the app — apps/greyin-b2b/src/app/jobs/[id]/apply/page.tsx — documented, pragmatic scope decision). Verified via resume-parsing.spec.ts passing end to end against the new private bucket.'],
  ['SEC-010', 'Storage & Secrets', 'CRITICAL', 'Storage bucket has no path scoping, MIME allowlist, or size limit', 'deployment/migrations/019_public_images_bucket.sql:20 (INSERT policy is just auth.role()=\'authenticated\')', 'Any logged-in user uploads an .html file served publicly as HTML from the Supabase origin (phishing/stored-content risk), or uploads unbounded volume to exhaust storage.', 'Fixed', '2026-08-24', '2026-08-24', 'public-images bucket (071) now has a 5MB limit and an image-only MIME allowlist, plus a path-scoped INSERT policy requiring the uploader\'s own user id as the first path segment. All 4 consuming uploader components (apps/greyin-b2b/FileUploader.tsx, apps/stackedge/greymatters-blog/freeagent-marketplace ImageUploader.tsx) updated to actually upload under that path (previously none of them did, which would have broken every image upload the moment the policy went live — caught before deploy, not after). Verified by typecheck across all 4 touched apps plus resume-parsing.spec.ts and the broader freeagent/stackedge/greymatters regression coverage.'],
  ['SEC-011', 'Storage & Secrets', 'CRITICAL', 'A real, signed service_role JWT is committed in the source tree', 'deployment/config/kong.yml:134 (expires 2036, comment admits "valid everywhere")', 'Anyone with repo or build-artifact access gets full RLS bypass on the entire database.', 'Fixed', '2026-08-24', '2026-08-26', 'Rotated at the user\'s explicit direction and timing (previously deliberately deferred pending exactly that decision). New JWT_SECRET generated (openssl rand); new anon and service_role JWTs freshly signed matching the exact existing payload shape (iss:supabase, ref:greyin, role, iat, exp=iat+10yrs) and independently verified via HMAC recomputation before ever being applied. deployment/.env, e2e/.env, and deployment/config/kong.yml all updated; original values backed up locally first. Found and fixed a real gotcha along the way: kong.yml is mounted into the Kong container via a plain bind mount, not a Swarm config object, so editing the file alone did not make Kong pick up the new consumer credentials -- Swarm did not detect a service-spec change, and Kong itself only reads its declarative config at startup, so an explicit `docker service update --force supabase_kong` was required to actually reload it. Confirmed via direct curl that the previously-committed (now rotated-away) key is cryptographically dead (401) while the new anon/service_role keys work end-to-end through Kong -> PostgREST (200). Full stack redeployed: supabase core stack (db/auth/rest/storage/studio/kong/analytics) via a rolling `docker stack deploy` (not the deploy script\'s destructive `stack rm`), frontend stack redeployed for the runtime SERVICE_ROLE_KEY env var, and all 6 apps rebuilt with the new NEXT_PUBLIC_SUPABASE_ANON_KEY build-time inlining. Verified with the full auth e2e suite post-rotation (sso, logout-flow all 6 apps, session-refresh-middleware, password-reset, security-regressions) -- 14/14 passing, proving real signup/login/SSO/logout/session-refresh/password-reset all work end-to-end on the rotated keys, not just curl-level key checks.'],
  ['SEC-012', 'XSS & Injection', 'CRITICAL', 'Stored XSS via unescaped JSON.stringify in a dangerouslySetInnerHTML JSON-LD block', 'apps/greyin-b2b/src/app/jobs/[id]/page.tsx:91 (employer-controlled job description/company fields)', 'An employer\'s job description containing a crafted </script><script> payload executes for every visitor to that job page.', 'Fixed', '2026-08-24', '2026-08-24', 'JSON.stringify output now has < replaced with \\u003c before injection (the standard mitigation for this exact class of JSON-LD/script-tag XSS — valid inside a JSON string, never interpreted as a tag boundary by the HTML parser). Verified via typecheck and deploy; no dedicated e2e assertion added (would require injecting a real XSS payload against prod, judged unnecessary given the fix is a well-established, narrow one-line mitigation).'],
  ['SEC-013', 'Data Access (RLS)', 'CRITICAL', 'collaborators view bypasses the RLS its own migration comment claims it relies on', 'deployment/migrations/059_collaborators.sql (no security_invoker=true; runs as view owner)', 'Any authenticated user reads every StackWorks collaboration and completed FlexPro order pairing on the platform, not just their own.', 'Fixed', '2026-08-24', '2026-08-24', 'ALTER VIEW public.collaborators SET (security_invoker = true) (067, PG15+ feature, confirmed supported on this instance\'s PG 15.1). The view now correctly runs as the querying role, so the underlying project_applications/gig_orders RLS actually applies as the original 059 comment claimed. Verified via direct SQL (an unrelated authenticated user sees 0 rows for pairs they\'re not part of) and worked-together-surfacing.spec.ts / reference-checks.spec.ts (both depend on this view, both still passing).'],
  ['SEC-014', 'Dependencies', 'HIGH', 'Next.js 14.2.4 has a critical CVE bundle (incl. CVSS 9.1 middleware auth bypass) fixed in 14.2.35', 'apps/*/package.json (all 6 apps)', 'CVE-2025-29927 (GHSA-f82v-jwr5-mffw) and a long tail of other high/moderate CVEs; practical impact of the middleware CVE specifically is lower here since real auth checks live in Server Component redirects, not middleware.ts.', 'Fixed', '2026-08-24', '2026-08-24', 'Bumped 14.2.4 -> 14.2.35 across all 6 apps\' package.json (same minor line, non-breaking). npm audit post-bump confirms the CRITICAL finding is gone (down to a handful of remaining "high" findings that need an out-of-scope 15.x major bump). Found and fixed one real regression along the way: apps/freeagent-marketplace/src/app/api/orders/upload-deliverable/route.ts (the only route in the entire codebase that processes a real multipart file upload via formData() and then redirects) started 404ing on the client after its redirect under 14.2.35 — fixed by pinning an explicit 303 status on all 8 NextResponse.redirect() calls in that file (confirmed via a full codebase search that no other route shares this exact upload+redirect shape). Verified via a 21-spec regression sweep across all 6 apps (payments, admin, discussions, reputation, SSO, logout, session-refresh) all passing, plus the specific order-lifecycle.spec.ts re-run clean in isolation after the fix.'],
  ['SEC-015', 'Auth & Admin', 'HIGH', 'Password-reset OTP is brute-forceable', 'apps/*/src/app/auth/reset-password-confirm/route.ts (all 6 apps); no GOTRUE_MAILER_OTP_EXP override (defaults 24h)', 'Request a reset for a victim\'s email, then brute-force the 6-digit code over the 24-hour window — no attempt counting or lockout.', 'Fixed', '2026-08-24', '2026-08-24', 'New shared rate_limit_attempts table + check_rate_limit()/record_rate_limit_attempt() SECURITY DEFINER RPCs (075) -- no direct grants on the table itself, only the RPCs can read/write it, so a client can\'t insert fake "success" rows to erase their own lockout. Wired into reset-password-confirm/route.ts across all 5 apps that have this flow (greyin-b2b, freeagent-marketplace, saltnpepper-community, stackedge, greymatters-blog -- greyin-hub has no signup/password-reset flow at all, SSO-only): 5 failed attempts locks further attempts for 20 minutes, even the genuinely correct code. Verified via direct SQL (independent per-key and per-action tracking, RLS confirmed blocking direct table access for anon while the RPCs work) and a new e2e/tests/greyin-b2b/password-reset.spec.ts lockout test (5 wrong codes, 6th attempt with the real code still rejected) plus the pre-existing legitimate-reset-flow test passing in all 5 apps, deployed and confirmed with zero regressions this round.'],
  ['SEC-016', 'Auth & Admin', 'HIGH', 'Session cookies are not httpOnly/secure, and scoped to the whole .greyin.net apex', 'apps/*/src/middleware.ts, lib/supabase/server.ts (all 6 apps) — @supabase/ssr 0.1.0 defaults, no override', 'A single XSS anywhere on any current or future *.greyin.net subdomain steals a token granting SSO to all 6 apps.', 'Fixed', '2026-08-24', '2026-08-25', 'secure:true, sameSite:\'lax\' added to the session cookie set/remove callbacks in all 6 apps\' middleware.ts (createServerClient, not the crash-prone createBrowserClient in lib/supabase/client.ts -- confirmed safe by re-reading that file\'s own comments before touching anything). httpOnly deliberately NOT added: every app\'s SiteHeader.tsx calls createClient().auth.getUser() client-side to decide Sign-In-vs-logged-in display; an httpOnly cookie is invisible to that call and would break every header platform-wide. Closing that exposure for real needs a genuine refactor (server-rendered auth-state props across all 6 apps), tracked here as a known, deliberate gap rather than forced through. Cookie domain stays apex-scoped (.greyin.net) since that\'s the real SSO mechanism, not an oversight. Staged rollout: greyin-b2b deployed and verified alone first (sso/logout-flow/session-refresh-middleware specs, 9/9 passed) before the remaining 5 apps. Found and fixed a real deploy mistake along the way, unrelated to the cookie change itself: saltnpepper-community\'s redeploy used a build-arg reading a nonexistent env var (SALTNPEPPER_DOMAIN instead of the real COMMUNITY_DOMAIN in deployment/.env), baking an empty NEXT_PUBLIC_SITE_URL into that build and breaking absoluteUrl() across its login/logout/signup routes (TypeError: Invalid URL, caught by a genuine test failure, confirmed via docker service logs, not assumed). Rebuilt with the correct var, redeployed, confirmed clean logs and a working /login route, then re-ran the full 9-test suite clean against the fully-corrected 6-app state.'],
  ['SEC-017', 'Auth & Admin', 'HIGH', 'No signup abuse protection anywhere (no captcha, no rate limit beyond GoTrue defaults)', 'All 6 apps\' signup flows; deployment/supabase-stack.yml has no GOTRUE_SECURITY_CAPTCHA_*', 'Mass automated account creation, directly amplifying SEC-001\'s admin-escalation risk.', 'Partially fixed — rate limit shipped; captcha deliberately deferred pending a user decision', '2026-08-24', '2026-08-25', 'DB-backed rate limit added to all 5 apps with a signup flow (greyin-b2b, greymatters-blog, freeagent-marketplace, saltnpepper-community, stackedge -- greyin-hub has no signup flow, correctly excluded), reusing 075\'s check_rate_limit/record_rate_limit_attempt RPCs with action=\'signup\'. Deliberately keyed by client IP (first entry of X-Forwarded-For, set by the Traefik edge) rather than email -- a mass-signup attacker uses a fresh email every time, so an email-keyed limit would never trigger, unlike SEC-015\'s password-reset case where the key genuinely is the target email. Every attempt is recorded with success=false regardless of GoTrue\'s actual outcome, repurposing the table\'s "failures" counter as a generic "attempts" counter for this action -- a successful account creation is exactly the abuse being limited here, so it has to count toward the cap too (documented inline given it reads unusual next to SEC-015\'s literal-failure semantics on the same table). Threshold: 10 signups/hour/IP. Verified via direct SQL (10th attempt from one IP locks out, unrelated IPs unaffected) and a live end-to-end run against prod (11 real POSTs to greyin-b2b\'s /auth/signup from a spoofed shared X-Forwarded-For: attempts 1-10 processed normally, 11th+ redirected with the lockout message). CORRECTION (2026-08-26): the original verification\'s "no orphaned accounts" claim was wrong -- the admin-API `filter=email.eq.X` check used to confirm that didn\'t actually filter, and 10 real unconfirmed @example.com accounts were left in prod; found and deleted via the admin API once the real GoTrue user list was checked directly. Separately, and more seriously: a few hours after shipping, the fully-rolled-out state was found to have a real availability bug -- every request through this deployment\'s Traefik resolved to the SAME X-Forwarded-For value, \'172.18.0.1\' (Traefik\'s own docker-bridge gateway address, not a real per-client IP; confirmed via `SELECT key, count(*) FROM rate_limit_attempts WHERE action=\'signup\' GROUP BY key`, one shared key already at 20 hits), discovered when it silently blocked the e2e suite\'s own signups. Left as shipped, this would have bucketed every real signup on the platform under one shared key -- locking out signup for EVERYONE after 10 total/hour, not 10 per attacker, a functional regression worse than the abuse it was meant to stop. Immediately cleared the bad rows and fixed the root cause in all 5 apps: the rate limit check/record is now skipped entirely whenever the resolved X-Forwarded-For value is private/link-local/absent (10.x, 172.16-31.x, 192.168.x, 127.x, 169.254.x, ::1, fc/fd IPv6 prefixes) rather than enforced against a meaningless shared key. Re-verified clean: rate_limit_attempts empty for signup after redeploy, and e2e/tests/cross-platform/security-regressions.spec.ts passing again (all signups un-blocked). NOT implemented: GOTRUE_SECURITY_CAPTCHA_* (hCaptcha/Turnstile/etc) -- genuinely closing the "no captcha" half of this finding requires picking and configuring a third-party provider and secret, an external-dependency decision this audit is not making unilaterally per standing guidance; tracked here as a separate, still-open sub-item pending that decision.'],
  ['SEC-018', 'Payments & Escrow', 'MEDIUM', 'No Razorpay webhook idempotency / replay protection', 'apps/freeagent-marketplace/src/app/api/webhooks/razorpay/route.ts:70-76,105-108', 'A replayed payment.captured event on an already-cancelled/refunded order flips it back to paid, enabling a second real refund.', 'Fixed', '2026-08-24', '2026-08-25', 'This route runs as service_role, which enforce_gig_order_transition (073) explicitly trusts and skips -- so nothing in the DB stopped a late/out-of-order webhook event from regressing an order\'s state, only app-layer guards can. Two changes: (1) idempotency -- razorpay_webhooks.event_id already had a real UNIQUE constraint, but a genuine redelivery previously fell through to the outer catch and returned 500 (causing Razorpay to retry the same event forever); now a 23505 unique-violation on insert returns 200 "already processed" immediately, a real no-op instead of a retry storm. Also fixed a latent bug in the same code path: the "mark as processed" update recomputed the fallback event_id with a fresh Date.now() instead of reusing the one actually inserted, silently updating zero rows whenever event.event_id was missing. (2) state guards -- handlePaymentCaptured/handleOrderPaid now only move an order to \'paid\' from \'pending\'/\'authorized\'; handleRefundCreated skips (no update, no duplicate email) if the order is already \'refunded\'. Verified via escrow-workflow.spec.ts and order-lifecycle.spec.ts passing clean after deploy (no regression to the real payment/refund/dispute flows).'],
  ['SEC-019', 'Payments & Escrow', 'MEDIUM', 'Non-timing-safe webhook/verify signature comparisons', 'apps/freeagent-marketplace/src/app/api/webhooks/razorpay/route.ts:21; api/orders/verify/route.ts:23; apps/greyin-b2b/src/app/api/webhooks/razorpay-subscriptions/route.ts:10', 'Plain === on hex digests is theoretically timing-attackable; signature verification itself is genuinely present at all 3 sites.', 'Fixed', '2026-08-24', '2026-08-25', 'Switched from === / !== to crypto.timingSafeEqual (with an explicit length check first, since timingSafeEqual throws rather than returning false on mismatched buffer lengths) at all 3 sites. No functional behavior change -- same accept/reject outcomes, just constant-time -- verified via the same escrow-workflow.spec.ts/order-lifecycle.spec.ts passing clean (both exercise real signature verification end to end).'],
  ['SEC-020', 'Data Access (RLS)', 'MEDIUM', 'company_reviews leaks reviewer_id, de-anonymizing "anonymous" reviews', 'deployment/migrations/058_company_reviews.sql:30-32 (SELECT USING (true) exposes the column; anonymity is UI-only)', '?select=reviewer_id,rating on the company_reviews endpoint de-anonymizes every review.', 'Fixed', '2026-08-24', '2026-08-24', 'Column-level REVOKE SELECT + narrow re-GRANT on (id, company_id, rating, review_text, created_at) only (067) — reviewer_id is now genuinely unreadable via any PostgREST query, not just absent from the app\'s own UI. Corrected in 072 after a regression: apps/greyin-b2b/src/app/companies/[id]/page.tsx legitimately needed to check "have I already reviewed this company" via a WHERE reviewer_id=auth.uid() filter, which the column revoke also blocked (column-level grants restrict any use of the column, including inside a WHERE clause, not just the SELECT list). Fixed with a narrow has_reviewed_company(company_id) SECURITY DEFINER RPC that checks only the caller\'s own membership, never exposing the column via a general grant. Verified via direct SQL (unrelated user\'s reviewer_id SELECT blocked; the new RPC works) and company-reviews.spec.ts passing end to end.'],
  ['SEC-021', 'Auth & Admin', 'MEDIUM', 'Open-redirect filter bypass via backslash in ?next= handling', 'apps/*/src/app/auth/login/route.ts (all 6 apps); lib/site-url.ts absoluteUrl()', '?next=/\\evil.com passes the startsWith("/") check; browsers normalize \\ to //, redirecting off-platform after login (phishing).', 'Fixed', '2026-08-24', '2026-08-25', 'Two real instances of the same bug found and fixed, not just one. (1) auth/login/route.ts\'s POST handler: startsWith(\'/\') && !startsWith(\'//\') missed the backslash case -- confirmed via node\'s URL constructor that /\\evil.com resolves to https://evil.com/ (browsers normalize \\ to / while parsing). Fixed by also rejecting any backslash in `next`. (2) login/page.tsx, found while the first round of fixes was already deploying: every app\'s `if (user) { redirect(next || \'/dashboard\') }` for an already-logged-in visitor had NO validation at all -- worse than the route.ts case, since visiting /login?next=https://evil.com while logged in redirects immediately, no form submission needed. Required a second, clean rebuild of all 6 apps to guarantee both fixes actually shipped together (the page.tsx edit landed mid-deploy of the route.ts-only first round). Both fixed identically across all 6 apps with the same relative/no-double-slash/no-backslash check. Verified via a new permanent e2e test in security-regressions.spec.ts covering both cases directly against prod, using page.request with maxRedirects:0 to inspect the server\'s raw Location header rather than actually navigating to a third-party domain -- passing clean.'],
  ['SEC-022', 'XSS & Injection', 'MEDIUM', 'Unescaped user input interpolated into PostgREST .or() filter strings', 'apps/greymatters-blog/src/app/search/page.tsx:25; apps/greyin-b2b/src/app/jobs/page.tsx:50; apps/freeagent-marketplace/src/app/gigs/page.tsx:60 and similar search pages', 'A comma or close-paren in a search query injects extra filter clauses; RLS caps the blast radius but the input is unsanitized in a query DSL.', 'Fixed', '2026-08-24', '2026-08-26', '13 sites across 7 files (search/page.tsx in all 5 apps that have one -- 2-3 .or() calls each -- plus greyin-b2b/jobs/page.tsx and freeagent-marketplace/gigs/page.tsx) had raw, unescaped user input interpolated into PostgREST .or() filter strings. Fixed with a small inline sanitizer (`s.replace(/[,()".{}\\\\]/g, \' \').trim()`) applied to the search term before every interpolation, stripping the characters that carry syntactic meaning in PostgREST\'s filter DSL (comma separates OR-conditions, parens group them, braces delimit the cs.{...} array-contains operator\'s literal value used by the people-search branch) rather than trying to selectively quote/escape them. Empirically confirmed the injection was real before fixing (a direct PostgREST call with an injected `,id.eq.<uuid>` condition measurably changed the result set) and confirmed clean after deploy (the same crafted query now renders as literal text -- "No results for x,id.eq.00000000-..." -- on the live search page, and normal single-word/multi-word searches are unaffected).'],
  ['SEC-023', 'Data Access (RLS)', 'MEDIUM', 'mentor_session_slots lets a mentor resell a paid seat and exposes recording_url pre-purchase', 'deployment/migrations/062_mentor_monetization.sql:344-347 (WITH CHECK only auth.uid()=mentor_id, unlike 056:63); 056:44-50', 'A mentor flips their own booked slot back to open and nulls gig_order_id, reselling an already-paid seat; recording_url is visible on any open-status slot.', 'Fixed', '2026-08-24', '2026-08-26', 'Two real gaps, migration 076. (1) Postgres combines multiple permissive UPDATE policies on the same table with OR at both USING and WITH CHECK -- 062\'s "attach a recording" policy\'s WITH CHECK only required auth.uid()=mentor_id, so it (not 056\'s stricter, status=\'open\'-only policy) governed any direct update to a \'booked\' row, letting a mentor PATCH status back to \'open\' and null gig_order_id, reselling an already-paid seat. Fixed with a BEFORE UPDATE trigger (same pattern as enforce_gig_order_transition/073) blocking any direct change to mentor_id/gig_id/gig_order_id, and blocking any status change away from \'booked\' outside the trusted book_mentor_slot()/service_role path. (2) recording_url was readable via the SELECT policy on any \'open\' slot regardless of purchase -- confirmed the app\'s own UI already rendered it safely (Server Component, URL never serialized to the client for non-owners) but a direct PostgREST call bypassing the UI got the real URL. Fixed via column-level REVOKE SELECT(recording_url) plus a new get_visible_recording_urls(slot_ids) SECURITY DEFINER RPC (mentor, original buyer, or genuine purchaser only); recording_price stays public since both app pages need it to render "Buy access". Updated both freeagent-marketplace pages that read recording_url (mentor-sessions/[gigId] and mentor-sessions/manage) to use the RPC instead of the raw column, using recording_price as the "has a recording" signal for the pages\' own filtering (column REVOKE blocks a column\'s use in a WHERE/filter, not just SELECT, so filtering by recording_url itself was no longer possible either). Verified via direct SQL in a rolled-back transaction: the resell exploit is blocked with the exact error, legitimate mentor recording-attach and own-open-slot-cancel still succeed, anon gets 0 rows reading the column directly, and the RPC correctly returns the URL for the real buyer but nothing for an unrelated stranger. tsc clean, deployed, smoke-checked.'],
  ['SEC-024', 'Data Access (RLS)', 'MEDIUM', 'create_job_referral_notification() has no real authorization beyond being logged in', 'deployment/migrations/040_job_referral_notification.sql:23-38', 'Attacker-controlled title/body pushed into any user\'s notification inbox.', 'Fixed', '2026-08-24', '2026-08-26', 'The real caller (apps/greyin-b2b/src/app/api/jobs/[id]/refer/route.ts) already derived job_title/referrer_name server-side, but the SECURITY DEFINER RPC itself was GRANTed to authenticated with no further checks -- a direct call bypassing that route could pass any p_job_title/p_referrer_name/p_job_id it wanted, including a nonexistent job. Migration 077 drops both text parameters entirely and derives them itself: referrer_name from the caller\'s own profiles row (auth.uid()), job_title from a real jobs row (RAISE EXCEPTION if p_job_id doesn\'t exist -- also closes the unvalidated-job-id gap). Old 4-arg signature REVOKEd and dropped so a stale/direct call can\'t hit the vulnerable version. Verified via direct SQL in a rolled-back transaction: a nonexistent job_id is rejected, and a legitimate call produces a notification whose title/body genuinely come from the real profile/job rows, not client input. App route updated to the new 2-arg call, tsc clean, deployed, smoke-checked.'],
  ['SEC-025', 'Data Access (RLS)', 'MEDIUM', 'subscription_plans has no RLS enabled at all', 'deployment/migrations/039_subscriptions.sql:38 — the only CREATE TABLE public.* across 66 migrations missing ENABLE ROW LEVEL SECURITY', 'Verify Supabase default grants don\'t let anon/authenticated write to pricing; currently unprotected by any policy.', 'Fixed', '2026-08-24', '2026-08-26', 'Turned out to be worse than the original MEDIUM rating: checked information_schema.role_table_grants directly and found anon/authenticated hold real INSERT/UPDATE/DELETE/TRUNCATE grants on this table (this instance\'s schema-wide default grants, not the SELECT-only the migration\'s own explicit GRANT line implied) -- with RLS disabled, those grants were the only gate, meaning any unauthenticated visitor could directly PATCH/DELETE the platform\'s real pricing rows via PostgREST. Confirmed via query that subscription_plans was the ONLY public table with RLS disabled platform-wide (no other instances of this class). Fixed via migration 078: ENABLE ROW LEVEL SECURITY plus a single public-SELECT policy, no write policies at all (there is no user-facing pricing-edit flow; plan changes go through service_role only, matching rate_limit_attempts\' no-direct-grants convention). Verified live against prod: anon SELECT still returns real pricing, and a live anon PATCH attempt on the starter plan\'s price now affects 0 rows (204, price confirmed unchanged) where it would previously have succeeded.'],
  ['SEC-026', 'Data Access (RLS)', 'MEDIUM', 'platform_search_index (granted to anon) leaks content past its own intended RLS', 'deployment/migrations/037_platform_search_index_v2.sql:45-61 (adds discussions.body, builder_projects to an anon-granted view; 024\'s own comment warned against this)', 'Anonymous visitors can read discussion bodies and builder projects that their own table RLS says should require authentication.', 'Fixed', '2026-08-24', '2026-08-26', 'The view ran with its owner\'s (postgres) privileges by default, bypassing every underlying table\'s RLS -- 024\'s own comment predicted exactly this risk for discussions/projects, but 037 added them without revisiting it. Also found a second, narrower leak in the same view along the way: posts\' RLS restricts published rows by view_audience (public/follower/verified_expert), but the view\'s WHERE only checked status=\'published\', so anon could read verified_expert-only posts through search too -- confirmed live against real prod data (46 published posts, only 11 view_audience=\'public\'; before the fix anon could read all 46 through the index). Fixed via ALTER VIEW ... SET (security_invoker = true) (079, same technique as SEC-013\'s collaborators fix) -- makes the view respect the querying role\'s own RLS instead of the owner\'s bypass. jobs (open/filled) and gigs (active) both already have genuinely public SELECT policies, so anonymous visitors\' legitimate search results are unaffected. Verified via direct SQL (anon now sees only the 11 public posts, 0 discussions/projects; an authenticated caller sees projects too) and ecosystem-search-coverage.spec.ts passing clean (confirms authenticated search still surfaces a real Salt & Pepper discussion).'],
  ['SEC-027', 'Data Access (RLS)', 'MEDIUM', 'activity_feed bypasses the view_audience=\'verified_expert\' restriction on posts', 'deployment/migrations/053_activity_feed.sql:110-122 (selects published posts unfiltered)', 'A post intended to be Verified-Expert-only still surfaces to any follower via the activity feed.', 'Fixed', '2026-08-24', '2026-08-26', 'Same root cause and same fix as SEC-026: this view\'s own migration comment (053) claimed "security comes entirely from each UNIONed table\'s own existing RLS still applying per-invoker" -- untrue as deployed, since security_invoker was never actually set, so the view ran with owner-bypass privileges the whole time despite the comment\'s assumption. Fixed via ALTER VIEW ... SET (security_invoker = true) (080). Verified via a rolled-back transaction with fabricated test data: a non-verified-expert follower could see a verified_expert-only post from a followed author before the fix, and correctly got 0 rows after. Confirmed no regression via the pre-existing activity-feed.spec.ts (stackedge) passing clean (a follower still correctly sees a followed Builder\'s public project update but not their private one).'],
  ['SEC-028', 'Dependencies', 'MEDIUM', 'nodemailer has a high-severity "email to unintended domain" CVE', 'apps/greyin-b2b/src/lib/email.ts; package.json nodemailer ^6.10.1', 'Confirm exact usage (password reset vs. notification-only) before assessing urgency; fixed by a dependency bump.', 'Fixed', '2026-08-24', '2026-08-26', 'Confirmed via npm audit: nodemailer <=9.0.0 carries 8 advisories in this codebase\'s range, the two most serious both HIGH -- GHSA-p6gq-j5cr-w38f (message-level raw option bypasses disableFileAccess/disableUrlAccess, enabling SSRF/arbitrary file read in the delivered message) and GHSA-rcmh-qjqh-p98v (addressparser ReDoS, plausibly reachable here since a recipient\'s display name is sometimes user-influenced text, e.g. a profile\'s full_name). Confirmed via reading the 3 affected apps\' lib/email.ts (freeagent-marketplace, greyin-b2b, greymatters-blog -- the only 3 using nodemailer) that actual usage is the plain createTransport({host,port,secure,auth}) + sendMail({from,to,subject,html}) shape, never raw/jsonTransport/custom transport-name, so none of the option-specific CVEs were directly exploitable through this codebase\'s own code, but the addressparser ReDoS parses every from/to string regardless. This is confirmed general-notification usage, not the GoTrue-native password-reset flow (that goes through Supabase Auth\'s own Go-based mailer, unrelated to this npm package) -- password-reset urgency assessment doesn\'t apply here, general notification-email availability does. Bumped 6.x -> 9.0.5 (semver-major) in all 3 apps; confirmed all 3 Dockerfiles already use node:20-alpine, satisfying 9.x\'s Node>=20 engine requirement in production (the EBADENGINE warning during local install only reflected the dev shell\'s Node 18). tsc clean, npm audit confirms nodemailer entries gone in all 3 apps, deployed and smoke-checked. Verified with a real end-to-end send (not just install/typecheck): ran the exact createTransport/sendMail code shape from lib/email.ts directly against the real production SMTP relay under Node 20 -- message accepted and delivered.'],
  ['SEC-029', 'Data Access (RLS)', 'LOW', 'Newsletter signup allows anonymous insert with WITH CHECK (true)', 'deployment/migrations/008_newsletter.sql:23-25', 'Spam/enumeration surface on the newsletter table.', 'Fixed', '2026-08-24', '2026-08-26', 'The app\'s own route already treated a 23505 (duplicate email) response as success, but that was UI-layer only -- a direct call to /rest/v1/newsletter_subscribers (bypassing the app entirely) still got a genuinely different response (409 vs 201) for an already-subscribed email, a real enumeration oracle RLS can\'t suppress since it\'s a UNIQUE constraint violation, not an authorization decision. Fixed via a new subscribe_to_newsletter(email) SECURITY DEFINER RPC (081) that does INSERT ... ON CONFLICT DO NOTHING and always returns the same thing either way; the direct table INSERT grant is revoked so the enumerable raw endpoint isn\'t reachable at all. App route updated to call the RPC instead. Verified via direct SQL: a direct table insert as anon now fails with permission denied, and the RPC succeeds identically for both a fresh and a duplicate email (confirmed exactly one row ever inserted).'],
  ['SEC-030', 'Data Access (RLS)', 'LOW', 'get_unread_message_count() is SECURITY DEFINER with public execute and no authz', 'deployment/migrations/005_direct_messaging.sql:83-94', 'Count-only information leak; low severity but PUBLIC EXECUTE on a function with no auth.uid() check.', 'Fixed', '2026-08-24', '2026-08-26', 'Confirmed via pg_proc.proacl that PUBLIC (including anon) genuinely had EXECUTE. Two real leaks, not just the one named in the finding: nothing stopped p_user_id from being someone else\'s id (read another user\'s unread count), and even pinning p_user_id to the caller, SECURITY DEFINER bypasses order_messages\' own RLS, so a caller who wasn\'t a real participant in p_order_id still got a real message count for that order (every message there, since none were "from" them) -- a leak of private order conversations they have no relation to. Fixed (082) by requiring auth.uid() = p_user_id and requiring the caller actually be the buyer or seller on that order (returns 0 for anyone else, matching the function\'s original "just a count" contract rather than erroring), plus REVOKE/GRANT closing the PUBLIC/anon execute grant. Confirmed via a full codebase grep that this function is currently dead code (no app caller anywhere), so the tightened authorization carries zero regression risk. Verified via direct SQL: anon is now fully blocked (permission denied), and both the wrong-user-id and non-participant cases safely return 0 rather than erroring or leaking.'],
  ['SEC-031', 'Privacy', 'LOW', 'A candidate can list any user as a professional reference without that person\'s consent', 'deployment/migrations/065_reference_checks.sql:89-91', 'By design per explicit user direction (2026-08-24 build) — noted here as a privacy consideration to track, not a bug to silently fix.', 'Acknowledged — by design, not being changed', '2026-08-24', '2026-08-26', 'Reviewed again during this audit\'s LOW-severity pass and confirmed unchanged: this is an intentional product decision made explicitly by the user during the original 2026-08-24 build, not a defect. No code or DB change made -- listed here only so the traceability doc shows every finding was actually looked at, not silently skipped.'],
  ['SEC-032', 'Data Access (RLS)', 'LOW', 'StackWorks skill-rating interaction gate is satisfiable by a single DM', 'deployment/migrations/054_skill_endorsements_and_ratings.sql:182-187', 'Enables drive-by 1-star ratings with minimal real interaction.', 'Fixed', '2026-08-24', '2026-08-26', 'The "informal chat" branch of the initial skill-rating gate only required a single direct_messages row to exist in a shared conversation -- satisfiable by the rater sending one throwaway DM the target never had to reply to, then immediately posting a public 1-star rating. Fixed (083) by requiring messages from BOTH participants, not just any single message\'s existence -- a real two-way exchange, still cheap/lightweight (no minimum count or length), just no longer a pure drive-by. Verified via direct SQL in a rolled-back transaction with fabricated test data: a one-sided DM is now rejected by RLS, and a genuine two-way exchange still succeeds. Found and fixed a real regression along the way: e2e/utils/admin.ts\'s createTestConversationWithMessage helper only ever seeded one side of the conversation (matching the old, vulnerable behavior) -- the pre-existing skill-ratings.spec.ts (stackedge) failed against the tightened policy until the helper was updated to seed both sides; both stackedge and freeagent skill-ratings.spec.ts pass clean after that fix.'],
  ['SEC-033', 'Payments & Escrow', 'LOW', 'payout_requests accepts a client-supplied amount at insert time', 'deployment/migrations/014_payouts.sql:37-39', 'The real balance check happens in application code, not the database — defense-in-depth gap rather than an active hole today.', 'Fixed', '2026-08-24', '2026-08-26', 'The app\'s own route (api/payouts/request/route.ts) already recomputes the real available balance server-side and rejects an over-limit request, but nothing at the DB level enforced this -- a direct PostgREST insert (bypassing the route) could put in a payout_requests row for any amount. Real-money exposure was already limited (an admin manually reviews and processes every request out-of-band, no live payout-rail integration exists), matching the finding\'s own "not an active hole today" framing, but a fraudulent row was still a real gap an inattentive approval could act on. Fixed (084) with a BEFORE INSERT trigger that recomputes the exact same formula the app route already uses (2% platform fee off completed orders as seller, minus already-requested non-rejected amounts) and rejects anything over the real available balance, regardless of which path the row came through. Verified via direct SQL: a wildly over-limit request is rejected with the real error, and a legitimate within-balance request still succeeds. Confirmed no regression via earnings-and-payouts.spec.ts passing clean.'],
  ['SEC-034', 'Hardening', 'LOW', '~36 of 69 SECURITY DEFINER functions lack an explicit SET search_path', 'Various migrations across the tree', 'Best-practice hardening gap; low real exploitability on a Supabase-managed instance.', 'Fixed', '2026-08-24', '2026-08-26', 'Re-checked via pg_proc.proconfig and found only 15 of the current 59 SECURITY DEFINER functions still missing an explicit search_path (down from the original ~36 -- most had already picked one up incidentally from functions written or touched earlier in this same audit). Closed the remaining 15 with plain ALTER FUNCTION ... SET search_path = public statements (085) -- pure metadata changes, no function bodies touched, zero behavior change for any legitimate caller. Included the 3 real Razorpay payment functions (capture_razorpay_payment, create_razorpay_order, process_razorpay_refund), so re-ran the core payment e2e specs (order-lifecycle.spec.ts, escrow-workflow.spec.ts) afterward as an extra precaution given the payment-path overlap -- both passing clean. Verified via direct SQL that 0 SECURITY DEFINER functions remain without search_path set.'],
  ['SEC-035', 'Payments & Escrow', 'LOW', 'No idempotency key or row lock on Razorpay refund calls', 'apps/freeagent-marketplace/src/app/api/orders/cancel/route.ts; api/admin/orders/resolve-dispute/route.ts', 'Two concurrent cancel requests on the same paid order could issue two refunds — a narrow race window.', 'Fixed', '2026-08-24', '2026-08-26', 'Confirmed the gap was real: refundPayment()\'s third argument only goes into Razorpay\'s notes field (plain metadata), not the real X-Razorpay-Idempotency-Key mechanism, so Razorpay itself doesn\'t dedupe concurrent refund calls either. Fixed with a claim-then-act pattern (086, same shape as this codebase\'s existing atomic-claim RPCs like book_mentor_slot): two new SECURITY DEFINER functions, claim_order_cancellation_refund and claim_order_dispute_refund, each do a real SELECT ... FOR UPDATE row lock plus the same buyer/admin+status authorization checks the app routes already had, and atomically mark refund_status=\'pending\' as a claim before returning success -- a second concurrent/replayed call blocks on the lock, then sees the claim already taken and is refused, so it never reaches Razorpay. refund_status was already a real column (014/020), previously write-only and unused for any check, so repurposing its \'pending\' value as the claim marker didn\'t collide with anything. Both app routes updated to call the claim RPC before invoking Razorpay, and to reset refund_status back to NULL on a Razorpay failure so a genuine retry stays possible. Verified via direct SQL (first claim succeeds, an immediate second claim attempt on the same order is refused) and escrow-workflow.spec.ts passing clean end to end (all 3 tests, including the real cancel-and-refund and admin-dispute-resolution flows that now go through the new claim RPCs).'],
  ['SEC-036', 'Dependencies', 'LOW', 'postcss / cookie / @supabase-ssr low-severity npm advisories', 'All 6 apps, transitive deps', 'Fixed incidentally by the Next.js version bump (SEC-014).', 'Fixed', '2026-08-24', '2026-08-26', 'postcss: fixed via an npm `overrides` field forcing Next.js\'s own nested/vendored copy up to 8.5.26 (required a full lockfile regeneration to actually take effect), verified clean in all 6 apps. @supabase/ssr: two attempts the same day. The first was a staged 1-app-then-5-more rollout (matching the SEC-016 precedent) -- rewrote get/set/remove to getAll/setAll (0.1.0 -> 0.12.5) in greyin-b2b alone, deployed, and it broke cross-app SSO live: the two library versions write session cookies in incompatible formats, so every app still on 0.1.0 failed to recognize a session started by the rewritten app (8 of 11 e2e tests failed, all cross-app, none same-app). Reverted immediately once confirmed, redeployed, reconfirmed 11/11 passing. Once the user confirmed no real end users are on the platform yet -- removing the live-session-compatibility constraint that made staging necessary -- reattempted as a true simultaneous all-6-apps cutover: rewrote all 6 apps\' middleware.ts + lib/supabase/server.ts via a verified script (each file\'s exact-match cookie-handling block confirmed found exactly once before substitution, cookieOptions.name and SEC-016\'s secure/sameSite/domain overrides preserved throughout, client.ts left unchanged), bumped all 6 to 0.12.5, built and deployed all 6 back-to-back with no staged pauses. Verified clean: npm audit shows both @supabase/ssr and cookie CVEs gone in all 6 apps, tsc clean, all 6 services converged with zero log errors, and the full auth e2e suite (14 tests -- signup, login, SSO across all 6 apps, logout, session-refresh, password-reset, security-regressions) passing clean end to end post-cutover. The failed staged attempt is preserved here rather than erased from the record: it is real, useful evidence that this specific library upgrade requires an atomic cutover, not incremental rollout, and that call was only safe to make because there were no live sessions to protect.'],
  ['SEC-037', 'Payments & Escrow', 'LOW', 'B2B subscription activation has no ownership check beyond the signature-bound ID', 'apps/greyin-b2b/src/app/api/subscriptions/checkout/verify/route.ts:52', 'Activates by razorpay_subscription_id with no separate ownership check; impact limited since that ID is itself signature-bound to the real payment.', 'Fixed', '2026-08-24', '2026-08-26', 'Added an explicit ownership check (a companies!inner(user_id) join confirming the authenticated caller owns the company that row belongs to) before activating a subscription, on top of the existing, unchanged Razorpay HMAC signature check -- closes the narrow gap where whoever held a valid signature triple for a subscription (by construction, whoever completed that specific Razorpay checkout) could activate it regardless of which authenticated session submitted the verify call. Also fixed a related completeness gap noticed while touching this code: activated_by/activated_at were always NULL, never actually set on activation -- now populated from the real caller. Verified via tsc --noEmit clean, deployed, and covered by the broader e2e regression pass (sso/logout-flow/security-regressions/escrow-workflow/order-lifecycle, 15/15 passing) -- no dedicated spec exists for this specific route, judged proportionate for a LOW-severity defense-in-depth addition where the real authorization boundary (the signature check) is unchanged.'],
  ['SEC-038', 'Dependencies', 'HIGH', 'Large batch of newly-disclosed Next.js CVEs (SSRF in Server Actions/rewrites, DoS, middleware/i18n bypass, cache poisoning, XSS) spanning 9.x-15.5.x', 'apps/*/package.json (all 6 apps), next 14.2.35', 'Real fix (next@16.3.3) is a 2-major-version jump touching App Router/Server Components/routing/middleware across the whole platform -- out of scope for a same-session fix given the regression risk and the precedent already set this session by the far smaller 14.2.4->14.2.35 patch bump breaking a real route. Deliberately not attempted; flagged as a dedicated follow-up effort needing its own planning and full regression pass, not bundled into this audit.', 'Open — flagged as out-of-scope, needs dedicated follow-up effort (2-major-version Next.js bump)', '2026-08-26', '', ''],
  ['SEC-039', 'Privacy', 'MEDIUM', 'peer_project_ratings\' first-shipped SELECT policy was USING(true) -- individual rater->ratee rows publicly queryable by anyone via PostgREST, following company_reviews\' (058) "anonymity is an app-layer convention, not access control" precedent', 'deployment/migrations/089_peer_projects.sql (original policy, superseded)', 'Caught before rollout, not in production: the user\'s own explicit requirement ("who gave what is not visible... anonymous aggregation for the end user") is materially stronger than company_reviews\' precedent -- any authenticated user could otherwise query ?rater_id=eq.X directly and see exactly who rated whom what, bypassing the app UI\'s anonymity entirely.', 'Fixed', '2026-09-02', '2026-09-02', 'Tightened (090) to USING(auth.uid()=rater_id OR employer/admin) before this feature was ever deployed to any real user -- a regular member now sees only ratings they personally gave; nobody else\'s, at the RLS layer itself, not by app convention. Employers/admins get purpose-built aggregate views instead (peer_rater_reliability, peer_project_reciprocity_flags), each independently gated via their own EXISTS(...role IN (employer,admin)) clause baked into the view\'s WHERE, since the tightened base-table policy alone no longer exposes rows broadly enough for an aggregate view to rely on. Verified via direct RLS simulation as rater/non-rater/employer/admin (all four cases: own-rating visible, others\'-rating hidden, employer aggregate visible, non-employer aggregate empty) and end-to-end via peer-projects.spec.ts.'],
]

const wb = XLSX.utils.book_new()

function addSheet(name, rows, colWidths) {
  const ws = XLSX.utils.aoa_to_sheet(rows)
  ws['!cols'] = colWidths.map((w) => ({ wch: w }))
  ws['!freeze'] = { xSplit: 0, ySplit: 1 }
  XLSX.utils.book_append_sheet(wb, ws, name)
}

addSheet('Functional Requirements', functional, [16, 26, 10, 70, 16, 12])
addSheet('Non-Functional Requirements', nonFunctional, [20, 12, 60, 60, 12])
addSheet('Traceability Matrix', traceability, [10, 40, 12, 16, 30, 45, 12, 14])
addSheet('Audit Roadmap', roadmap, [4, 60, 18, 28, 10, 14, 60])
addSheet('Pending Partial Deferred', pending, [36, 26, 70, 40, 14])
addSheet('Security Findings', securityFindingsRaw, [9, 18, 10, 55, 50, 55, 10, 12, 12, 60])

const outPath = process.argv[2]
XLSX.writeFile(wb, outPath)
console.log('Wrote', outPath)
console.log('Functional requirements:', functional.length - 1)
console.log('Non-functional requirements:', nonFunctional.length - 1)
console.log('Security findings:', securityFindingsRaw.length - 1)
console.log('Traceability rows:', traceability.length - 1)
console.log('Audit roadmap items:', roadmap.length - 1)
console.log('Pending/partial/deferred:', pending.length - 1)
